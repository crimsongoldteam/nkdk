---
name: config-add-item
description: Создание нового объекта в YAML-конфигурации 1С через схему проекта. Используй, когда нужно добавить в конфигурацию новый объект, например справочник, документ, перечисление, форму или другой YAML-файл проекта, и перед записью нужно получить YAML-сводку схемы командой nkdk schema.
---

# Добавление объекта в конфигурацию

Скилл помогает добавить новый объект в YAML-конфигурацию проекта. Главный источник формы файла — компактная YAML-сводка схемы, полученная консольной командой проекта.

## Входные данные

Если пользователь не дал всё сразу, выясняй по одному пункту и сначала исследуй проект:

1. Корень YAML-проекта.
2. Тип объекта и имя, например `Справочник/Товары` или `Документ/Заказ`.
3. Какой файл создаётся: обычно `Свойства.yaml`, для форм — `Формы/<ИмяФормы>/Форма.yaml`.
4. Минимальные бизнес-свойства, которые должны быть заполнены сразу.

## Каталоги объектов

Для верхнеуровневых объектов YAML-каталог совпадает с `itemTypePrefix` правила. Если пользователь называет объект по-русски, строй путь как `<YAML-каталог>/<Имя>/Свойства.yaml`, затем подтверждай форму файла через `nkdk schema`.

| Объект 1С | YAML-каталог | XML-каталог |
| --- | --- | --- |
| Бизнес-процесс | `БизнесПроцесс` | `BusinessProcesses` |
| Бот | `Бот` | `Bots` |
| Web-сервис | `WebСервис` | `WebServices` |
| WebSocket-клиент | `WebSocketКлиент` | `WebSocketClients` |
| HTTP-сервис | `HTTPСервис` | `HTTPServices` |
| WS-ссылка | `WSСсылка` | `WSReferences` |
| XDTO-пакет | `ПакетXDTO` | `XDTOPackages` |
| Внешний источник данных | `ВнешнийИсточникДанных` | `ExternalDataSources` |
| Группа команд | `ГруппаКоманд` | `CommandGroups` |
| Документ | `Документ` | `Documents` |
| Журнал документов | `ЖурналДокументов` | `DocumentJournals` |
| Задача | `Задача` | `Tasks` |
| Константа | `Константа` | `Constants` |
| Критерий отбора | `КритерийОтбора` | `FilterCriteria` |
| Нумератор документов | `Нумератор` | `DocumentNumerators` |
| Обработка | `Обработка` | `DataProcessors` |
| Общая картинка | `ОбщаяКартинка` | `CommonPictures` |
| Общая команда | `ОбщаяКоманда` | `CommonCommands` |
| Общая форма | `ОбщаяФорма` | `CommonForms` |
| Общий макет | `ОбщийМакет` | `CommonTemplates` |
| Общий модуль | `ОбщийМодуль` | `CommonModules` |
| Общий реквизит | `ОбщийРеквизит` | `CommonAttributes` |
| Определяемый тип | `ОпределяемыйТип` | `DefinedTypes` |
| Отчет | `Отчет` | `Reports` |
| Параметр сеанса | `ПараметрСеанса` | `SessionParameters` |
| Параметр функциональных опций | `ПараметрФункциональныхОпций` | `FunctionalOptionsParameters` |
| Перечисление | `Перечисление` | `Enums` |
| План видов расчета | `ПланВидовРасчета` | `ChartsOfCalculationTypes` |
| План видов характеристик | `ПланВидовХарактеристик` | `ChartsOfCharacteristicTypes` |
| План обмена | `ПланОбмена` | `ExchangePlans` |
| План счетов | `ПланСчетов` | `ChartsOfAccounts` |
| Подписка на событие | `ПодпискаНаСобытие` | `EventSubscriptions` |
| Подсистема | `Подсистема` | `Subsystems` |
| Последовательность | `Последовательность` | `Sequences` |
| Регистр бухгалтерии | `РегистрБухгалтерии` | `AccountingRegisters` |
| Регистр накопления | `РегистрНакопления` | `AccumulationRegisters` |
| Регистр расчета | `РегистрРасчета` | `CalculationRegisters` |
| Регистр сведений | `РегистрСведений` | `InformationRegisters` |
| Регламентное задание | `РегламентноеЗадание` | `ScheduledJobs` |
| Роль | `Роль` | `Roles` |
| Сервис интеграции | `СервисИнтеграции` | `IntegrationServices` |
| Справочник | `Справочник` | `Catalogs` |
| Стиль | `Стиль` | `Styles` |
| Хранилище настроек | `ХранилищеНастроек` | `SettingsStorages` |
| Функциональная опция | `ФункциональнаяОпция` | `FunctionalOptions` |
| Элемент стиля | `ЭлементСтиля` | `StyleItems` |
| Язык | `Язык` | `Languages` |

## Получение схемы

Построй путь будущего файла относительно корня YAML-проекта и сначала получи компактную YAML-сводку:

```bash
pnpm --filter @nakidka/cli dev schema "<relative-yaml-file>" --project "<yaml-project-dir>"
```

Если в окружении доступна установленная команда `nkdk`, можно использовать эквивалент:

```bash
nkdk schema "<relative-yaml-file>" --project "<yaml-project-dir>"
```

Если целевой YAML-каталог не является pnpm workspace и команда выше отвечает `No projects found`,
запускай CLI из каталога workspace, где доступен `@nakidka/cli`, но `--project` всегда указывай на целевой
YAML-проект:

```bash
cd /Users/nikita/git/nakidka-core
pnpm --filter @nakidka/cli dev schema "<relative-yaml-file>" --project "<yaml-project-dir>"
```

Такой запуск использует CLI как инструмент получения схемы; не читай исходный код `nakidka-core`, если пользователь
этого не просил.

После первой ориентации работай срезами, чтобы не перечитывать всю схему:

```bash
nkdk schema InputField --required --keys
nkdk schema InputField --required
nkdk schema InputField --keys
nkdk schema InputField --keys "путь|вид"
nkdk schema InputField --search "путь|тип" --keys
nkdk schema InputField --search "путь|тип"
nkdk schema InputField --search ПутьКДанным --exact
```

Обычный порядок такой: сначала посмотри обязательные ключи, затем найди нужные поля через `--keys` или
`--search`, затем запроси точную подсказку по одному полю через `--search <ИмяПоля> --exact`.

Примеры путей, которые понимает команда:

```text
Справочник/Товары/Свойства.yaml
Документ/Заказ/Свойства.yaml
Документ/Заказ/Формы/ФормаДокумента/Форма.yaml
```

`nkdk schema` пока понимает не все верхнеуровневые каталоги из таблицы. Надёжно поддержаны `Справочник`,
`Документ`, `Перечисление`, `Обработка`, `ЖурналДокументов`, `HTTPСервис`, `РегистрСведений`,
`РегистрНакопления`, `ПланОбмена` и формы внутри поддержанных объектов.

Если YAML-сводки недостаточно или нужно увидеть исходную JSON Schema, используй явный JSON-режим:

```bash
nkdk schema InputField --json-schema
nkdk schema InputField --json-schema --inline
nkdk schema "<relative-yaml-file>" --project "<yaml-project-dir>" --json-schema
```

Сводка может показывать внешние ссылки вида:

```yaml
$ref: nkdk://schema/MetadataAttribute
```

Чтобы уточнить такую ссылку, сначала запроси YAML-сводку по короткому имени последнего сегмента URI.
Если используешь `pnpm`, выполняй команду из workspace, где доступен `@nakidka/cli`:

```bash
cd /Users/nikita/git/nakidka-core
pnpm --filter @nakidka/cli dev schema "MetadataAttribute"
```

или используй установленную команду:

```bash
nkdk schema "MetadataAttribute"
```

Если сводка вложенной схемы снова содержит `$ref` или не хватает деталей, повторяй дозапрос по имени нужной схемы.
Полный JSON-разворот одним ответом используй только для отладки:

```bash
nkdk schema "<relative-yaml-file>" --project "<yaml-project-dir>" --json-schema --inline
```

Если команда отвечает ошибкой `Ожидались пути вида ...`, не правь YAML вслепую: уточни относительный путь по существующей структуре проекта или соседнему объекту.

## Создание файла

1. Найди 1-2 соседних объекта того же типа и повтори их раскладку каталогов.
2. По схеме определи обязательные поля (`required`) и допустимые значения (`enum`, `anyOf`, `const`).
3. Создай минимальный YAML: только обязательные поля и явно заданные пользователем свойства.
4. Не выдумывай значения для полей, смысл которых не следует из схемы, соседей или запроса.
5. Если создаёшь форму или дочерний объект, проверь, нужны ли дополнительные каталоги и файлы по аналогии с соседом.

## Проверка

После записи файла снова получи схему для созданного пути и проверь, что файл соответствует ей доступным в проекте валидатором или ближайшим существующим сценарием проверки.

Если задача затрагивает синхронизацию с XML, дополнительно предложи прогнать:

```bash
pnpm --filter @nakidka/cli dev sync "<yaml-project-dir>" "<xml-dir>"
```

Не запускай `sync`, если пользователь не дал XML-каталог или это может перезаписать важные файлы.
