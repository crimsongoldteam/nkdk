# Контроль свойств расширения конфигурации в YAML

## Цель

Значимые режимы свойств заимствованных объектов хранятся в YAML. Снимок
конфигурации не хранит эти режимы и не участвует в их восстановлении.

Спецификация не меняет устройство снимка, его файловый формат или используемое
хранилище.

## Пользовательская модель

Конфигуратор показывает режим свойства значком. YAML сохраняет именно этот
режим, а не буквальное устройство `xr:PropertyState`:

| Значок в Конфигураторе | Смысл | YAML |
|---|---|---|
| Значка нет | Собственное значение объекта расширения | Обычное значение без тега |
| Сине-красный | Значение контролируется | Обычное значение без тега |
| Синий | Значение проверяется с предупреждением | `!проверять` |
| Оранжевый | Значение изменяется расширением | `!изменять` или раздел `Изменять` |
| Значок режима снят | Заимствованное свойство не участвует в расширении | Свойство отсутствует |

Отсутствие значка и снятый значок режима — разные случаи. У собственного
свойства режима контроля нет, однако его значение принадлежит расширению и
сохраняется в YAML. У заимствованного свойства снятый значок означает, что
свойство не участвует в расширении и отсутствует в YAML.

Это различие определяется не формой значения и не именем YAML-поля, а ролью
свойства в конкретном metadata-объекте. Например, `Синоним` конфигурации
расширения является собственным значением без значка, а `Синоним`
заимствованного справочника — модифицируемым свойством с оранжевым значком.

Набор доступных значков зависит от свойства. YAML проверяется по той же модели:

- `ОбъектРасширяемойКонфигурации` допускает отсутствие, обычный контроль и
  `!проверять`; изменять это свойство нельзя;
- основные формы и другие свойства, для которых Конфигуратор показывает только
  оранжевый значок, допускают лишь отсутствие или `!изменять`;
- `Тип` допускает составные режимы только у тех объектов, для которых это
  разрешает платформа; в остальных правилах он остаётся обычным контролируемым
  свойством;
- элементы `Состав` общего реквизита допускают только отсутствие или обычный
  сине-красный контроль без `!проверять` и `!изменять`;
- для остальных свойств допустимые режимы определяются видом metadata-объекта и
  версией платформы.

Недопустимый для свойства режим является ошибкой валидации YAML.

## Возможности свойств

Каждая пара «вид metadata-объекта + свойство» задаёт набор разрешённых
возможностей. Это не взаимоисключающие классы:

| Возможность | Представление в YAML |
|---|---|
| Собственное значение | Обычное значение; теги режимов запрещены |
| Контролировать | Обычное значение без тега |
| Проверять | `!проверять` |
| Изменять | `!изменять`; для вынесенного содержимого — раздел `Изменять` |
| Составные режимы | Режим задаётся отдельным частям значения |

Одно свойство может разрешать несколько возможностей. Например, `ДлинаКода`,
`ДопустимаяДлинаКода` и `ДлинаНаименования` плана обмена допускают обычный
контроль, `!проверять` и `!изменять`. Элементы `Состав` общего реквизита
допускают только обычный контроль, а основные формы — только `!изменять`.

Наличие одной возможности не означает наличия другой. Допустимый набор нельзя
надёжно вывести только из XML-имени свойства, YAML-имени или типа его значения.
Он задаётся правилами конкретного вида metadata-объекта.
Повторяющиеся группы объявляются один раз в общих фрагментах `rules.ts`, а
особенности конкретного объекта — рядом с его правилами. Полная плоская таблица
всех свойств не дублируется в отдельном центральном модуле.

## Доступность свойств заимствованного объекта

Обычный metadata-объект и его заимствованное представление имеют разные наборы
доступных свойств. Наличие свойства в общих правилах объекта не означает, что
его можно записать у заимствованного объекта.

Для каждого вида заимствованного объекта `rules.ts` задаёт закрытый список
доступных свойств. В него входят:

- собственные свойства экземпляра расширения;
- свойства основной конфигурации, которые Конфигуратор разрешает
  контролировать, проверять или изменять;
- служебные свойства, необходимые для связи с основной конфигурацией.

Любое другое YAML-поле запрещено, даже если оно допустимо для обычного объекта
того же вида. Проверка выполняется в два этапа:

1. Поле должно быть доступно у заимствованного объекта.
2. Выбранный режим должен входить в набор возможностей этого поля.

Например, если свойство существует в полном `rules.ts` справочника, но его нет
на панели свойств заимствованного справочника, импорт такого YAML завершается
ошибкой. Отсутствующее поле не передаётся в XML и не восстанавливается из
снимка.

Таблицы ниже являются подтверждёнными частями этих закрытых списков. Строка
`—` означает, что возможность недоступна; `да` — доступна.

Подтверждённые свойства заимствованного справочника:

| Возможности | Свойства |
|---|---|
| Собственные значения | `Имя`, `Комментарий` |
| Контролировать и проверять | `ОбъектРасширяемойКонфигурации`, `Иерархический`, `ВидИерархии`, `Владельцы`, `ДопустимаяДлинаКода` |
| Контролировать, проверять и изменять | `ДлинаКода`, `ДлинаНаименования`, `ТипКода` |
| Изменять | `Синоним`, `МодульОбъекта`, `МодульМенеджера`, `Предопределенные`, основные формы, представления объекта и списка, `Пояснение` |

### Реквизит справочника

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый либо флажок снят | Не изменять либо изменять | Свойство отсутствует либо `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Тип` | У подтипов разные значки | Составные режимы подтипов | Список подтипов с отдельными тегами |
| `Формат` | Оранжевый, если свойство применимо к типу | Изменять | `!изменять` |
| `ФорматРедактирования` | Оранжевый, если свойство применимо к типу | Изменять | `!изменять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |

У `СтроковыйРеквизитСИндексом` подтип `Дата` проверяется, а `Булево`
изменяется. У `РеквизитСправочника` подтип `Дата` контролируется, а `Булево`
изменяется. Оба случая используют общую запись из раздела «Составной тип».

Остальные свойства обычного реквизита, отсутствующие на показанных панелях,
недоступны у этих заимствованных реквизитов и запрещены в их YAML.

### Табличная часть справочника

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Синий | Проверять | `!проверять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычной табличной части недоступны у показанной
заимствованной табличной части и запрещены в её YAML.

### Реквизит табличной части справочника

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Синий | Проверять | `!проверять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Тип` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычного реквизита табличной части недоступны у показанного
заимствованного реквизита и запрещены в его YAML.

### Форма справочника

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Синий | Проверять | `!проверять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Форма` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |

Остальные пользовательские свойства обычной формы недоступны у показанной
заимствованной формы и запрещены в её YAML. Служебный `ТипФормы` может
присутствовать в XML, но не становится полем YAML.

### Команда справочника

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Группа` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ТипПараметраКоманды` | Нет | Собственное значение команды расширения | Обычное значение |
| `МодульКоманды` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |
| `Картинка` | Синий | Проверять | `!проверять` |

У команды справочника `Картинка` только проверяется. Это отличается от команд
плана обмена и критерия отбора, у которых на исследованных снимках `Картинка`
изменяется. Остальные свойства обычной команды недоступны у показанной
заимствованной команды и запрещены в её YAML.

### Макет справочника

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Флажок снят | Изменение не включено; доступные режимы не подтверждены | Свойство отсутствует |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Макет` | Флажок снят | Изменение содержимого не включено; доступные режимы не подтверждены | Раздел `Изменять` отсутствует |
| `ТипМакета` | Свойство отсутствует на панели | Недоступно как пользовательское свойство заимствованного макета | Запрещено |

`ТипМакета` (`TemplateType`) присутствует в XML как служебная копия типа
основного макета, но не становится полем YAML. Остальные свойства обычного
макета недоступны у показанного заимствованного макета и запрещены в его YAML.

Подтверждённые свойства конфигурации расширения:

| Возможности | Свойства |
|---|---|
| Собственные значения | `Имя`, `Синоним`, `Комментарий`, `НазначениеРасширенияКонфигурации`, `ПоддерживатьСоответствиеОбъектамПоВнутреннимИдентификаторам`, `ПрефиксИмен`, `ВариантВстроенногоЯзыка`, сведения о поставщике и версии |
| Контролировать и проверять | `ОбъектРасширяемойКонфигурации`, `ОсновнойРежимЗапуска`, `НазначенияИспользования`, `ОсновнойЯзык`, режимы совместимости и использования возможностей платформы |
| Изменять | `ОсновныеРоли`, командные интерфейсы и рабочие области, `ОсновнойСтиль`, `Логотип`, `Заставка`, основные формы конфигурации |

Подтверждённые свойства заимствованной подсистемы:

| Возможности | Свойства |
|---|---|
| Собственные значения | `Имя`, `Комментарий` |
| Контролировать и проверять | `ОбъектРасширяемойКонфигурации` |
| Изменять | `Синоним`, `Состав`, `КомандныйИнтерфейс`, `Пояснение`, `Картинка` |

Подтверждённые свойства заимствованного общего модуля:

| Возможности | Свойства |
|---|---|
| Собственные значения | `Имя`, `Комментарий` |
| Контролировать и проверять | `ОбъектРасширяемойКонфигурации`, `Глобальный`, `Клиент`, `Сервер`, `ВнешнееСоединение`, `ВызовСервера`, `ПовторноеИспользованиеВозвращаемыхЗначений` |
| Изменять | `Синоним`, `Модуль` |

Подтверждённые свойства заимствованного параметра сеанса:

| Возможности | Свойства |
|---|---|
| Собственные значения | `Имя`, `Комментарий` |
| Контролировать и проверять | `ОбъектРасширяемойКонфигурации`, `Тип` |
| Изменять | `Синоним` |

Подтверждённые свойства заимствованной роли:

| Возможности | Свойства |
|---|---|
| Собственные значения | `Имя`, `Комментарий` |
| Контролировать и проверять | `ОбъектРасширяемойКонфигурации` |
| Изменять | `Синоним`, `Права` |

Подтверждённые свойства заимствованного общего реквизита:

| Свойство | Собственное | Контролировать | Проверять | Изменять | Примечание |
|---|:---:|:---:|:---:|:---:|---|
| `Имя` | да | — | — | — | Значение объекта расширения |
| `Комментарий` | да | — | — | — | Значение объекта расширения |
| `Синоним` | — | — | — | да | Оранжевый значок |
| `ОбъектРасширяемойКонфигурации` | — | да | да | — | Сине-красный или синий значок |
| `Тип` | — | да | да | — | На показанном общем реквизите изменение недоступно |
| `Состав` | — | — | — | да | Оранжевый значок свойства целиком |
| Элемент `Состав` | — | да | — | — | Только сине-красный флажок внутри списка |
| `АвтоИспользование` | — | да | да | — | Сине-красный или синий значок |
| `РазделениеДанных` | — | да | да | — | Сине-красный или синий значок |
| `Подсказка` | — | — | — | да | Оранжевый значок |

Подтверждённые свойства заимствованного плана обмена:

| Свойство | Собственное | Контролировать | Проверять | Изменять | Примечание |
|---|:---:|:---:|:---:|:---:|---|
| `Имя` | да | — | — | — | Значение объекта расширения |
| `Комментарий` | да | — | — | — | Значение объекта расширения |
| `Синоним` | — | — | — | да | Оранжевый значок |
| `ОбъектРасширяемойКонфигурации` | — | да | да | — | Изменение недоступно |
| `Состав` | — | — | — | да | Оранжевый значок |
| `МодульОбъекта` | — | — | — | да | Оранжевый значок |
| `МодульМенеджера` | — | — | — | да | Оранжевый значок |
| `ДлинаКода` | — | да | да | да | Доступен любой из трёх значков |
| `ДопустимаяДлинаКода` | — | да | да | да | Доступен любой из трёх значков |
| `ДлинаНаименования` | — | да | да | да | Доступен любой из трёх значков |
| Основные формы | — | — | — | да | Оранжевый значок |
| Представления объекта и списка | — | — | — | да | Оранжевый значок |
| `Пояснение` | — | — | — | да | Оранжевый значок |

Три разрешённых режима скалярных свойств плана обмена используют одну форму
значения и отличаются только тегом:

```yaml
ДлинаКода: 11
ДопустимаяДлинаКода: !проверять Переменная
ДлинаНаименования: !изменять 45
```

### Реквизит плана обмена

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Тип` | Сине-красный | Контролировать; дополнительные режимы определяются правилом составного типа | Обычное значение либо составная запись типа |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |
| `ФормаВыбора` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычного реквизита недоступны у показанного заимствованного
реквизита плана обмена и запрещены в его YAML.

### Табличная часть плана обмена

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычной табличной части недоступны у показанной
заимствованной табличной части и запрещены в её YAML.

Реквизит табличной части имеет тот же подтверждённый закрытый список, что и
реквизит плана обмена: `Имя`, `Синоним`, `Комментарий`,
`ОбъектРасширяемойКонфигурации`, `Тип`, `Подсказка` и `ФормаВыбора`.

### Форма плана обмена

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Форма` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |

Остальные свойства обычной формы недоступны у заимствованной формы и запрещены
в её YAML.

### Команда плана обмена

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Группа` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ТипПараметраКоманды` | Нет | Собственное значение команды расширения | Обычное значение |
| `МодульКоманды` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |
| `Картинка` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычной команды недоступны у заимствованной команды и
запрещены в её YAML.

### Макет плана обмена

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Макет` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |

Остальные свойства обычного макета недоступны у заимствованного макета и
запрещены в его YAML.

### Критерий отбора

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `МодульМенеджера` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Тип` | Нет | Собственное значение критерия расширения | Обычное значение |
| `Состав` | Оранжевый | Изменять список целиком | `!изменять` |
| Элемент `Состав` | Сине-красный | Контролировать включение объекта | Обычный элемент списка без тега |
| `ИспользоватьСтандартныеКоманды` | Оранжевый | Изменять | `!изменять` |
| `ОсновнаяФорма` | Оранжевый | Изменять | `!изменять` |
| `ПредставлениеСписка` | Оранжевый | Изменять | `!изменять` |
| `РасширенноеПредставлениеСписка` | Оранжевый | Изменять | `!изменять` |
| `Пояснение` | Оранжевый | Изменять | `!изменять` |

У `Состав` критерия отбора повторяется вложенная модель общего реквизита:
оранжевый режим относится к свойству целиком, а сине-красные флажки — к
выбранным объектам внутри списка. `!проверять` и `!изменять` на отдельном
элементе состава запрещены.

Остальные свойства обычного критерия отбора недоступны у показанного
заимствованного критерия и запрещены в его YAML.

### Форма критерия отбора

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Форма` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |

Остальные свойства обычной формы недоступны у заимствованной формы критерия
отбора и запрещены в её YAML.

### Команда критерия отбора

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Группа` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ТипПараметраКоманды` | Нет | Собственное значение команды расширения | Обычное значение |
| `МодульКоманды` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |
| `Картинка` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычной команды недоступны у заимствованной команды
критерия отбора и запрещены в её YAML.

### Подписка на событие

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Источник` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычной подписки на событие недоступны у показанной
заимствованной подписки и запрещены в её YAML.

### Регламентное задание

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |

Другие свойства обычного регламентного задания на панели заимствованного
объекта отсутствуют и запрещены в его YAML.

### Бот

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Модуль` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Картинка` | Оранжевый | Изменять | `!изменять` |

Остальные свойства обычного бота недоступны у показанного заимствованного бота
и запрещены в его YAML.

### Функциональная опция

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый, если свойство доступно | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Хранение` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Состав` при булевом `Хранении` | Оранжевый | Изменять список объектов, доступностью которых автоматически управляет опция | `!изменять` |
| `Состав` при небулевом `Хранении` | Недоступен | Запрещено | Поле отсутствует |

Доступность `Состава` определяется типом значения `Хранения`, а не именем
функциональной опции. В подтверждённом примере
`ФункциональнаяОпцияБулево` хранится в булевой константе
`Константа.КонстантаБулево`, поэтому `Состав` изменяем. Опция
`ФункциональныеОпцииПолный` хранится в реквизите
`Справочник.СправочникПолный.Реквизит.РеквизитСправочника` типа `Дата`, поэтому
`Состав` недоступен.

Небулевое значение функциональной опции может использоваться в прикладной
логике через явное получение значения, но не задаёт однозначного состояния
«включено/выключено» для автоматического управления объектами метаданных.
Следовательно, `rules.ts` проверяет доступность `Состава` по разрешённому типу
объекта, указанного в `Хранении`; отдельный перечень имён функциональных опций
не требуется.

### Параметр функциональных опций

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Использование` | Свойство отсутствует на панели | Недоступно | Запрещено |

`Использование` (`Use`) существует у обычного параметра функциональных опций,
но отсутствует у его заимствованного представления. Поэтому оно не передаётся
в XML расширения, не восстанавливается из снимка и запрещено в YAML
заимствованного объекта.

### Определяемый тип

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Тип` | Сине-красный | Контролировать; дополнительные режимы определяются правилом составного типа | Обычное значение либо составная запись типа |

Других смысловых свойств у обычного определяемого типа нет. Служебные
идентификаторы из `InternalInfo` не являются полями YAML и восстанавливаются
из снимка.

### Хранилище настроек

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `МодульМенеджера` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `ОсновнаяФормаСохранения` | Оранжевый | Изменять | `!изменять` |
| `ОсновнаяФормаЗагрузки` | Оранжевый | Изменять | `!изменять` |
| `ВспомогательнаяФормаСохранения` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ВспомогательнаяФормаЗагрузки` | Свойство отсутствует на панели | Недоступно | Запрещено |

Дочерние формы и макеты могут быть заимствованы независимо от выбора основных
форм. Их имена перечисляются как дочерние объекты хранилища, а режим вынесенного
содержимого хранится на самом дочернем объекте.

### Форма хранилища настроек

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Форма` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `ТипФормы` | Свойство отсутствует на панели | Недоступно как пользовательское свойство заимствованной формы | Запрещено |

`ТипФормы` (`FormType`) может присутствовать в XML заимствованной формы как
служебная копия характеристики основной формы. Это не делает его доступным
полем YAML.

### Макет хранилища настроек

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Макет` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `ТипМакета` | Свойство отсутствует на панели | Недоступно как пользовательское свойство заимствованного макета | Запрещено |

`ТипМакета` (`TemplateType`) может присутствовать в XML как служебная копия
характеристики основного макета, но не задаётся в YAML заимствованного объекта.

### Общая команда

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Группа` | Доступны сине-красный, синий и оранжевый | Контролировать, проверять или изменять | Обычное значение, `!проверять` или `!изменять` |
| `ТипПараметраКоманды` | Нет | Собственное значение команды расширения | Обычное значение |
| `МодульКоманды` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |
| `Картинка` | Оранжевый | Изменять | `!изменять` |
| `Представление` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `СочетаниеКлавиш` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ВключатьВСодержаниеСправки` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `РежимИспользованияПараметра` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ИзменяетДанные` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ПоведениеПриНедоступностиОсновногоСервера` | Свойство отсутствует на панели | Недоступно | Запрещено |

Все три режима `Группы` подтверждены именно для общей команды. Для команд
других владельцев набор возможностей не наследуется автоматически и остаётся
заданным их собственными таблицами.

### Группа команд

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Категория` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |
| `Картинка` | Оранжевый | Изменять | `!изменять` |
| `Представление` | Свойство отсутствует на панели | Недоступно | Запрещено |

Наличие всех трёх режимов у свойства `Группа` общей команды не переносится на
свойство `Категория` самой группы команд: для него подтверждён только
сине-красный контроль.

### Общая форма

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Форма` | Оранжевый | Изменять вынесенное содержимое формы и её модуля | Раздел `Изменять` |
| `РасширенноеПредставление` | Оранжевый | Изменять | `!изменять` |
| `Пояснение` | Оранжевый | Изменять | `!изменять` |
| `ТипФормы` | Свойство отсутствует на панели | Недоступно как пользовательское свойство | Запрещено |
| `ВключатьСправкуВСодержание` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `НазначенияИспользования` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ИспользоватьСтандартныеКоманды` | Свойство отсутствует на панели | Недоступно | Запрещено |

`ТипФормы` может присутствовать в XML заимствованной формы как служебная копия
характеристики основной формы, но не задаётся в YAML.

### Общий макет

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Макет` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `ВидМакета` | Свойство отсутствует на панели | Недоступно как пользовательское свойство | Запрещено |

`ВидМакета` (`TemplateType`) может присутствовать в XML заимствованного макета
как служебная копия характеристики основного макета, но не задаётся в YAML.

### Общая картинка

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Картинка` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `ДоступностьДляВыбора` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ДоступностьДляОформления` | Поле показано неактивным, без значка | Недоступно | Запрещено |

В 1С 8.3.27 `ДоступностьДляОформления` можно редактировать только при
включённой `ДоступностиДляВыбора`. У заимствованной общей картинки
`ДоступностьДляВыбора` не выводится, поэтому зависимое поле остаётся
неактивным. Оба свойства обычной картинки запрещены в YAML заимствованного
объекта.

### Пакет XDTO

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ПространствоИмен` (`URI пространства имен`) | Доступны сине-красный, синий и оранжевый | Контролировать, проверять или изменять | Обычное значение, `!проверять` или `!изменять` |
| `Пакет` | Доступны сине-красный, синий и оранжевый | Контролировать, проверять или изменять вынесенное содержимое | Файл `Package.bin`; при необходимости раздел `Проверять` или `Изменять` |

Для вынесенного `Пакета` наличие `Package.bin` без упоминания в служебном
разделе означает обычный сине-красный контроль. `Проверять: [Пакет]` задаёт
синий режим, `Изменять: [Пакет]` — оранжевый. Одновременно указать свойство в
обоих разделах нельзя.

### Web-сервис

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ПакетыXDTO` | Оранжевый | Изменять | `!изменять` |
| `Модуль` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `ПространствоИмен` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ИмяФайлаДескриптора` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ПовторноеИспользованиеСеансов` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ВремяЖизниСеанса` | Свойство отсутствует на панели | Недоступно | Запрещено |

### Операция Web-сервиса

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ТипВозвращаемогоЗначенияXDTO` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `МожетБытьНеопределено` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `Транзакционный` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ИмяПроцедуры` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `РежимУправленияБлокировкойДанных` | Свойство отсутствует на панели | Недоступно | Запрещено |

### Параметр операции Web-сервиса

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ТипЗначенияXDTO` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `МожетБытьНеопределено` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `НаправлениеПередачи` | Свойство отсутствует на панели | Недоступно | Запрещено |

### HTTP-сервис

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Модуль` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `КорневойURL` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ПовторноеИспользованиеСеансов` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ВремяЖизниСеанса` | Свойство отсутствует на панели | Недоступно | Запрещено |

### Шаблон URL HTTP-сервиса

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Шаблон` | Свойство отсутствует на панели | Недоступно | Запрещено |

### Метод HTTP-сервиса

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `HTTPМетод` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `Обработчик` | Свойство отсутствует на панели | Недоступно | Запрещено |

### WS-ссылка

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `URL` | Свойство отсутствует на панели | Недоступно | Запрещено |

### WebSocket-клиент

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Модуль` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Предопределенный` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `АвтоПодключение` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `АдресСервера` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `Пользователь` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `Пароль` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `Заголовки` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ИспользоватьПроксиОС` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `ИспользоватьАутентификациюОС` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `Таймаут` | Свойство отсутствует на панели | Недоступно | Запрещено |

### Сервис интеграции

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Модуль` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `АдресВнешнегоСервисаИнтеграции` | Свойство отсутствует на панели | Недоступно | Запрещено |
| `Каналы` | Дочерние объекты в дереве | Заимствованные каналы сохраняются; новый собственный канал расширения разрешён | Обычный элемент раздела `Каналы` без служебного тега |

### Элемент стиля

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | По умолчанию флажок снят; при включении оранжевый | Не изменять либо изменять | Свойство отсутствует либо `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Тип` (`Вид`) | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Значение` | По умолчанию флажок снят; при включении оранжевый | Не изменять либо изменять | Свойство отсутствует либо `!изменять` |

Снятый флажок `Синоним` или `Значение` означает отсутствие модификации, а не
пустое значение. Импорт не создаёт для такого свойства узел YAML, экспорт не
добавляет его в XML расширения.

### Стиль

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Стиль` (`Style.xml`) | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |

### Язык

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `КодЯзыка` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |

### Константа

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `МодульМенеджера` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `Тип` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `МодульМенеджераЗначения` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |
| `ОсновнаяФорма` | Оранжевый | Изменять | `!изменять` |
| `РасширенноеПредставление` | Оранжевый | Изменять | `!изменять` |
| `Пояснение` | Оранжевый | Изменять | `!изменять` |
| `Формат` | Оранжевый | Изменять | `!изменять` |
| `ФорматРедактирования` | Оранжевый | Изменять | `!изменять` |
| `Подсказка` | Оранжевый | Изменять | `!изменять` |
| `ИспользоватьСтандартныеКоманды` | Свойство отсутствует на показанной части панели | Недоступно | Запрещено |

Свойства ниже `Подсказки`, не попавшие на снимок, требуют отдельного
подтверждения и этой таблицей не классифицируются.

### Документ

| Свойство | Значок на скриншоте | Допустимые режимы | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное поле |
| `Синоним` | Оранжевый | `Изменять` | Секция `Изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное поле |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | `Контролировать` или `Проверять`; `Изменять` запрещено | Обычное значение либо `!проверять` |
| `МодульОбъекта` | Оранжевый | `Изменять` | Секция `Изменять` |
| `МодульМенеджера` | Оранжевый | `Изменять` | Секция `Изменять` |
| `Нумератор` | Оранжевый; допускается переключение | `Проверять` или `Изменять` | Обычное поле с тегом `!проверять` либо секция `Изменять` |
| `ТипНомера` | Оранжевый; допускается переключение | `Проверять` или `Изменять` | Обычное поле с тегом `!проверять` либо секция `Изменять` |
| `ДлинаНомера` | Оранжевый; допускается переключение | `Проверять` или `Изменять` | Обычное поле с тегом `!проверять` либо секция `Изменять` |
| `ДопустимаяДлинаНомера` | Оранжевый; допускается переключение | `Проверять` или `Изменять` | Обычное поле с тегом `!проверять` либо секция `Изменять` |
| `Периодичность` | Оранжевый | `Изменять` | Секция `Изменять` |
| `КонтрольУникальности` | Оранжевый | `Изменять` | Секция `Изменять` |
| `Движения` | Нет | Собственное значение | Обычное поле |
| `ОсновнаяФормаОбъекта` | Оранжевый | `Изменять` | Секция `Изменять` |
| `ОсновнаяФормаСписка` | Оранжевый | `Изменять` | Секция `Изменять` |
| `ОсновнаяФормаВыбора` | Оранжевый | `Изменять` | Секция `Изменять` |
| `ПредставлениеОбъекта` | Оранжевый | `Изменять` | Секция `Изменять` |
| `РасширенноеПредставлениеОбъекта` | Оранжевый | `Изменять` | Секция `Изменять` |
| `ПредставлениеСписка` | Оранжевый | `Изменять` | Секция `Изменять` |
| `РасширенноеПредставлениеСписка` | Оранжевый | `Изменять` | Секция `Изменять` |
| `Пояснение` | Нет | Собственное значение | Обычное поле |

Таблица фиксирует свойства, видимые на предоставленном скриншоте; свойства документа ниже границы снимка требуют отдельного подтверждения.

### Нумератор документов

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `ТипНомера` | На снимке оранжевый; доступны все три вида флажка | Контролировать, проверять или изменять | Обычное значение, `!проверять` или `!изменять` |
| `ДлинаНомера` | На снимке оранжевый; доступны все три вида флажка | Контролировать, проверять или изменять | Обычное значение, `!проверять` или `!изменять` |
| `ДопустимаяДлинаНомера` | На снимке оранжевый; доступны все три вида флажка | Контролировать, проверять или изменять | Обычное значение, `!проверять` или `!изменять` |
| `Периодичность` | Оранжевый | Изменять | `!изменять` |
| `КонтрольУникальности` | Оранжевый | Изменять | `!изменять` |

### Последовательность

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `МодульНабораЗаписей` | Оранжевый | Изменять вынесенное содержимое | Раздел `Изменять` |

### Измерение последовательности

| Свойство | Значок на скриншоте | Доступность в заимствованном объекте | YAML |
|---|---|---|---|
| `Имя` | Нет | Собственное значение | Обычное значение |
| `Синоним` | Оранжевый | Изменять | `!изменять` |
| `Комментарий` | Нет | Собственное значение | Обычное значение |
| `ОбъектРасширяемойКонфигурации` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |
| `Тип` | Сине-красный | Контролировать или проверять; изменять нельзя | Обычное значение либо `!проверять` |

### Структура заимствованных сервисов

| Заимствованный родитель | Дочерний объект | Ограничение |
|---|---|---|
| Web-сервис | Операция | Новую операцию добавить нельзя; в YAML допустимы только операции, уже заимствованные из расширяемой конфигурации |
| Операция Web-сервиса | Параметр | Новый параметр добавить нельзя; в YAML допустимы только параметры, уже заимствованные из расширяемой конфигурации |
| HTTP-сервис | Шаблон URL | Новый шаблон добавить нельзя; в YAML допустимы только шаблоны, уже заимствованные из расширяемой конфигурации |
| Шаблон URL HTTP-сервиса | Метод | Новый метод добавить нельзя; в YAML допустимы только методы, уже заимствованные из расширяемой конфигурации |
| Сервис интеграции | Канал | Новый собственный канал расширения добавить можно; это исключение из ограничений Web- и HTTP-сервисов |

Ограничение относится только к родителю с принадлежностью `Заимствованный`.
Объект, созданный самим расширением, сохраняет обычные правила создания
дочерних объектов.

Элементы `Состав` общего реквизита образуют вложенную контролируемую группу:
для каждого объекта доступно только включение с сине-красным флажком либо
исключение из состава.

На подтверждённых видах заимствованных объектов повторяется базовая группа:

- `Имя` и `Комментарий` — собственные свойства объекта расширения;
- `Синоним` — модифицируемое свойство;
- `ОбъектРасширяемойКонфигурации` — контролируемое свойство;
- модуль, форма, командный интерфейс, картинка и другое вынесенное
  содержимое — модифицируемые свойства;
- для остальных свойств нет режима по умолчанию: они разрешаются только после
  подтверждения для конкретного вида объекта.

Эта группа применяется только к заимствованным metadata-объектам. Корневая
конфигурация расширения имеет собственный набор локальных свойств; например,
её `Синоним` не является модифицируемым.

Перечни выше фиксируют только показанные и выгруженные примеры. Они не
считаются общей матрицей остальных metadata-объектов.

## Снимки из TemporaryItems

Все 111 исходных PNG от 2026-08-12 проверены в полном размере. Таблицы
фиксируют показанный на снимках режим поля заимствованного объекта. Поле без
значка хранит собственное значение объекта расширения.

| Вид заимствованного объекта | Собственные поля без значка | Сине-красный значок | Жёлтый значок |
|---|---|---|---|
| Регистр расчёта | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Периодичность`, `ПериодДействия`, `БазовыйПериод`, `ПланВидовРасчета`, `График`, `ЗначениеГрафика`, `ДатаГрафика` | `Синоним`, `МодульНабораЗаписей`, `МодульМенеджера`, основная форма списка, представления списка, `Пояснение` |
| Регистр бухгалтерии | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `ПланСчетов`, `ДлинаУточненияПериода` | `Синоним`, `МодульНабораЗаписей`, `МодульМенеджера`, основная форма списка, представления списка, `Пояснение` |
| Регистр накопления | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `ВидРегистра` | `Синоним`, `МодульНабораЗаписей`, `МодульМенеджера`, основная форма списка, представления списка, `Пояснение` |
| Регистр сведений | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Периодичность`, `РежимЗаписи` | `Синоним`, `МодульНабораЗаписей`, `МодульМенеджера`, формы записи и списка, представления записи и списка, `Пояснение` |
| План видов расчёта | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, параметры кода и наименования, период действия, зависимость от базы | `Синоним`, модули, основные формы, представления, `Пояснение` |
| План счетов | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `ДлинаПорядка`, `ВидыСубконто` | `Синоним`, параметры кода и наименования, `Предопределенные`, модули, основные формы, представления, `Пояснение` |
| План видов характеристик | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Иерархический` | `Синоним`, параметры кода и наименования, `Предопределенные`, модули, основные формы, представления, `Пояснение` |
| Обработка | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, модули, основная форма, расширенное представление, `Пояснение` |
| Отчёт | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `ОсновнаяСхемаКомпоновкиДанных`, модули, основные формы, расширенное представление, `Пояснение` |
| Перечисление | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `МодульМенеджера`, основные формы, представления, `Пояснение` |
| Журнал документов | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `РегистрируемыеДокументы` | `Синоним`, `МодульМенеджера`, основная форма, представления списка, `Пояснение` |
| Документ | `Имя`, `Комментарий`, `Движения` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Нумератор`, `ТипНомера`, `ДлинаНомера`, `ДопустимаяДлинаНомера`, `Периодичность`, `КонтрольУникальности`, модули, основные формы, представления, `Пояснение` |
| Последовательность | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `МодульНабораЗаписей` |
| Константа | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Тип` | `Синоним`, `МодульМенеджера`, `МодульМенеджераЗначения`, основная форма, расширенное представление, форматы, `Подсказка`, `Пояснение` |
| Справочник | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Иерархический`, `ВидИерархии`, `Владельцы`, параметры кода и наименования | `Синоним`, `Предопределенные`, модули, основные формы, представления, `Пояснение` |
| Элемент стиля | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Вид` | `Синоним`, `Значение` |
| Сервис интеграции | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Модуль` |
| WebSocket-клиент | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Модуль` |
| WS-ссылка | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним` |
| HTTP-сервис | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Модуль` |
| Метод и шаблон URL HTTP-сервиса | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним` |
| Параметр Web-сервиса | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним` |
| Общий макет и макет хранилища настроек | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Макет` |
| Общая форма | `Имя`, `Комментарий`, `Пояснение` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Форма`, `РасширенноеПредставление` |
| Форма хранилища настроек | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Форма` |
| Группа команд | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Категория` | `Синоним`, `Подсказка`, `Картинка` |

Повторяющиеся подчинённые виды объектов:

| Вид | Собственные поля без значка | Сине-красный значок | Жёлтый значок |
|---|---|---|---|
| Реквизит, измерение, ресурс | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Тип`; у отдельных видов также `Ведущее`, `Балансовый` или `ПризнакУчета` | `Синоним`, форматы, `Подсказка`, `ФормаВыбора` |
| Табличная часть | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Подсказка` |
| Форма | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Форма`, расширенное представление |
| Команда | `Имя`, `Комментарий`, `ТипПараметраКоманды` | `ОбъектРасширяемойКонфигурации`, `Группа` | `Синоним`, `МодульКоманды`, `Подсказка`, `Картинка` |
| Макет | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним`, `Макет` |
| Значение перечисления | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним` |
| Измерение последовательности | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Тип` | `Синоним` |
| Перерасчёт | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации` | `Синоним` |
| Графа журнала документов | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Ссылки` | `Синоним` |
| Признак учёта плана счетов | `Имя`, `Комментарий` | `ОбъектРасширяемойКонфигурации`, `Тип` | `Синоним`, форматы, `Подсказка` |

Если у поля показан сине-красный значок, для него всегда доступны обычный
контроль и зелёный режим `!проверять`; жёлтый режим `!изменять` запрещён. Поля
с жёлтым значком по умолчанию допускают только `!изменять`. Исключения, в
которых жёлтое поле допускает также сине-красный и зелёный режимы, добавляются
в правила только после отдельного подтверждения.

## Теги YAML

`!проверять` и `!изменять` — локальные теги формата NKDK, как и существующий
`!xml`. Тег добавляет к смысловому значению режим свойства и не меняет тип
самого значения:

```yaml
ОбъектРасширяемойКонфигурации: !проверять РеквизитСсылка
ОсновнаяФормаОбъекта: !изменять ФормаЭлемента
Формат: !проверять "ДФ=dd.MM.yyyy"
```

Пустое изменяемое значение записывается без искусственного значения:

```yaml
ОсновнаяФормаОбъекта: !изменять
```

Разборщик и сериализатор YAML поддерживают кириллицу в именах этих тегов как
расширение формата NKDK. Новый YAML всегда записывает канонические имена
`!проверять` и `!изменять`.

Обычный сине-красный контроль отдельного тега не имеет:

```yaml
Тип: ХранилищеЗначения
```

Существующий `!xml` сохраняет прежний смысл: это исключительное точное
XML-значение, которое нельзя восстановить из смыслового YAML. Он не обозначает
режим контроля свойства.

## Составной тип

Если у подтипов разные значки, `Тип` записывается списком. Тег ставится на тот
подтип, к которому относится соответствующий режим.

Проверяемый и изменяемый подтипы:

```yaml
Тип:
  - !проверять Дата
  - !изменять Булево
```

Контролируемый и изменяемый подтипы:

```yaml
Тип:
  - Справочник.СправочникПолный
  - !изменять Справочник.СправочникРеквизит
```

Даже единственный изменяемый подтип записывается списком:

```yaml
Тип:
  - !изменять Строка(10)
```

Эта форма означает `MultiState`, содержащий только `ExtendValue`, и точно
восстанавливает выгрузку Конфигуратора.

Пустая контролируемая часть сохраняется пустым элементом списка без тега:

```yaml
Тип:
  - []
  - !изменять Справочник.ПроектныеЗадачи
```

## Состав общего реквизита

`Состав` заимствованного общего реквизита является изменяемым структурным
свойством с оранжевым значком. Каждый объект внутри состава имеет только два
состояния: контроль снят либо установлен обычный сине-красный контроль. Синий
и оранжевый режимы для отдельного объекта недоступны.

В YAML `!изменять` ставится на список целиком, а элементы не получают тегов
режима:

```yaml
Состав: !изменять
  - Объект: Справочники.СправочникОбщиеРеквизиты
    Использование: Использовать
  - Объект: ПланыВидовРасчета.ПланРасчетаВсеСвойства
    Использование: НеИспользовать
```

Отсутствие объекта в списке означает снятый флажок. `!проверять` запрещён для
списка целиком; `!проверять` и `!изменять` запрещены на отдельном элементе.

`Состав` не использует модель `MultiState`: это отдельное правило сложного
свойства `CommonAttributeContent`. Вложенные сине-красные элементы являются
ожидаемой копией соответствующих элементов основной конфигурации. Если
контролируемый элемент или его параметры `Использование` и
`УсловноеРазделение` отличаются от основной конфигурации, проверка
совместимости отказывает в применении расширения.

## Вынесенное содержимое

У форм, модулей, прав, командных интерфейсов, картинок и другого вынесенного
содержимого значение находится в отдельном YAML-, BSL-, XML- или двоичном
файле. Режим относится к свойству metadata-объекта, а не к внешнему файлу.

Оранжевый режим задаётся в разделе `Изменять` YAML-файла самого объекта:

```yaml
Изменять:
  - Форма
  - МодульОбъекта
```

Содержимое продолжает храниться отдельно, например:

```text
Формы/ФормаЭлемента/Форма.yaml
МодульОбъекта.bsl
```

Наличие имени в разделе `Изменять` означает оранжевый режим. Отсутствие имени
означает, что специальный режим для свойства не задан. Синий и сине-красный
режимы этим разделом не представляются.

Если таблица конкретного вида объекта разрешает синий режим вынесенного
свойства, он задаётся отдельным разделом `Проверять`:

```yaml
Проверять:
  - Пакет
```

Наличие внешнего файла без имени свойства в `Проверять` или `Изменять`
означает обычный сине-красный контроль. Отсутствие файла означает отсутствие
свойства. Одно имя нельзя одновременно поместить в оба раздела. Раздел
`Проверять` запрещён для вынесенных свойств, которым таблица разрешает только
оранжевый режим.

Подтверждённые имена вынесенных свойств:

| YAML-имя | XML-имя |
|---|---|
| `Форма` | `Form` |
| `Модуль` | `Module` |
| `МодульОбъекта` | `ObjectModule` |
| `МодульМенеджера` | `ManagerModule` |
| `Права` | `Rights` |
| `Состав` | `Content` |
| `ПредопределенныеДанные` | `Predefined` |
| `КомандныйИнтерфейс` | `CommandInterface` |
| `РабочаяОбластьНачальнойСтраницы` | `HomePageWorkArea` |
| `КомандныйИнтерфейсОсновногоРаздела` | `MainSectionCommandInterface` |
| `КартинкаОсновногоРаздела` | `MainSectionPicture` |
| `Логотип` | `Logo` |
| `Заставка` | `Splash` |
| `Пакет` | `Package` |

Перечень не является общей матрицей возможностей платформы. Допустимость имени
определяется правилами конкретного вида metadata-объекта и версией платформы.

## XML-модель

Платформа определяет пять значений `xr:PropertyState`:

| XML-состояние | Смысл |
|---|---|
| `NotSet` | Режим не задан |
| `Checked` | Значение контролируется |
| `Notify` | Значение проверяется с предупреждением |
| `Extended` | Значение изменяется расширением |
| `MultiState` | Части значения имеют разные режимы |

Соответствие пользовательского режима XML не всегда буквальное:

- обычный сине-красный контроль обычно хранится обычным XML-значением без
  `xr:PropertyState`;
- синий режим хранится как `xr:State=Notify`;
- оранжевый режим обычного свойства хранится как `xr:State=Extended`;
- у свойств, допускающих только оранжевый режим, платформа может выводить режим
  из самого присутствия XML-свойства без `xr:PropertyState`;
- разные режимы частей типа хранятся как `xr:State=MultiState` и значение
  `xr:ExtendedProperty` с `xr:CheckValue`, `xr:NotifyValue` и
  `xr:ExtendValue`.

Например, пустая изменяемая основная форма объекта выгружается без
`xr:PropertyState`:

```xml
<DefaultObjectForm/>
```

Синий режим свойства выгружается явно:

```xml
<xr:PropertyState>
  <xr:Property>ExtendedConfigurationObject</xr:Property>
  <xr:State>Notify</xr:State>
</xr:PropertyState>
```

Составной тип с контролируемой и изменяемой частями:

```xml
<xr:PropertyState>
  <xr:Property>Type</xr:Property>
  <xr:State>MultiState</xr:State>
</xr:PropertyState>
<Type xsi:type="xr:ExtendedProperty">
  <xr:CheckValue xsi:type="v8:TypeDescription">
    <v8:Type>cfg:CatalogRef.СправочникПолный</v8:Type>
  </xr:CheckValue>
  <xr:ExtendValue xsi:type="v8:TypeDescription">
    <v8:Type>cfg:CatalogRef.СправочникРеквизит</v8:Type>
  </xr:ExtendValue>
</Type>
```

## Предопределённые данные

`Изменять: [ПредопределенныеДанные]` относится ко всему свойству `Predefined`.
Режимы отдельных заимствованных предопределённых элементов хранятся внутри их
смысловой YAML-структуры и не подменяются этим разделом.

## Импорт XML → YAML

1. Собственное XML-свойство импортируется обычным значением и не получает тег
   режима.
2. Отсутствующее заимствованное XML-свойство не создаёт YAML-поля.
3. Обычное контролируемое XML-значение импортируется без тега.
4. `Notify` превращается в `!проверять` на значении.
5. Обычный `Extended` превращается в `!изменять` на значении.
6. Синий режим вынесенного свойства добавляет его имя в раздел `Проверять`.
7. Оранжевый режим вынесенного свойства добавляет его имя в раздел `Изменять`.
8. `MultiState` разбирается в список обычных, `!проверять` и `!изменять`
   частей.
9. Ни один режим не записывается в снимок конфигурации.

## Экспорт YAML → XML

1. Обычное значение собственного свойства создаёт XML-свойство без режима
   контроля.
2. Обычное значение контролируемого свойства создаёт контролируемое
   XML-свойство в платформенном представлении этого свойства.
3. `!проверять` создаёт синий режим в платформенном XML-представлении.
4. `!изменять` создаёт оранжевый режим в платформенном XML-представлении.
5. Список частей типа создаёт `xr:ExtendedProperty` и
   `xr:State=MultiState`.
6. Имена из раздела `Проверять` создают синий режим вынесенных свойств.
7. Имена из раздела `Изменять` создают оранжевый режим вынесенных свойств.
8. Порядок `xr:PropertyState` определяется порядком XML-свойств в `rules.ts`, а
   не порядком YAML.

## Диагностики

- Неизвестный локальный тег является ошибкой YAML.
- Поле, существующее у обычного metadata-объекта, но недоступное у его
  заимствованного представления, является ошибкой валидации YAML.
- Тег режима на собственном свойстве является ошибкой валидации.
- Режим, недоступный для данного свойства, является ошибкой валидации.
- Имя в разделе `Проверять`, не зарегистрированное для данного вида
  metadata-объекта как проверяемое вынесенное свойство, является ошибкой.
- Имя в разделе `Изменять`, не зарегистрированное для данного вида
  metadata-объекта, является ошибкой валидации.
- Тег на части значения разрешён только правилам, поддерживающим составное
  представление.
- Одно свойство нельзя одновременно пометить `!изменять` и добавить в раздел
  `Изменять`.
- Значимый режим, который импорт ещё не умеет представить в YAML, нельзя молча
  пропускать или сохранять в снимке: импорт завершается диагностикой.

## Сбор полной матрицы правил

Исследуемая матрица относится к платформе 1С 8.3.27. Для другой версии
платформы допустимые режимы должны быть подтверждены отдельно.

XML-выгрузки достаточно, чтобы установить выбранный режим, но недостаточно,
чтобы узнать все режимы, разрешённые Конфигуратором для свойства. Поэтому для
каждого ещё не исследованного вида metadata-объекта необходимы:

1. Полная панель свойств заимствованного объекта с видимыми значками возле
   каждого свойства.
2. XML-выгрузка этого объекта после сохранения выбранных режимов.
3. Для неоднозначных свойств — отдельные выгрузки после каждого доступного
   состояния: снято, сине-красный, синий и оранжевый.
4. Для `Тип` — примеры одиночного и составного типа, включая разные режимы
   подтипов.
5. Номер версии платформы и режим совместимости расширения, на которых получен
   пример.

Базовая конфигурация и выгрузка расширения должны передаваться вместе: по ним
проверяются соответствие объектов, исходное значение свойства и точное XML-
представление результата. Скриншот нужен только для определения разрешённых
вариантов; выбранное состояние и XML-представление извлекаются из выгрузки.

Сначала исследуются общие семейства свойств — идентификация заимствованного
объекта, локальные `Имя` и `Комментарий`, представления, формы, модули,
вынесенное содержимое и `Тип`. После этого для каждого вида metadata-объекта
остаются только его специальные свойства. Так матрица собирается по группам,
без ручного переписывания одинаковых правил для каждого объекта.

## Подтверждённые примеры

Новая выгрузка
`e2e/fixtures/xml/cfe/all-extension/Catalogs/СправочникВладелец.xml`
подтверждает:

- снятый флажок `Объект расширяемой конфигурации` удаляет
  `ExtendedConfigurationObject`;
- синий флажок этого свойства и `Тип` создаёт `xr:State=Notify`;
- пустой флажок `Тип` удаляет XML-свойство;
- оранжевый флажок всего `Тип` создаёт `MultiState` только с `ExtendValue`;
- разные значки подтипов создают `CheckValue`, `NotifyValue` и `ExtendValue`.

Новая выгрузка того же справочника подтверждает, что оранжевая пустая
`Основная форма объекта` записывается как `<DefaultObjectForm/>` без
`xr:PropertyState`.

В ранее исследованных `/Users/nikita/git/sed_xml/cfe` и
`/Users/nikita/git/round-trip-compact/cfe` найдено 163 записи:

| Состояние | Количество |
|---|---:|
| `Extended` | 161 |
| `MultiState` | 2 |
| `Checked` | 0 |
| `Notify` | 0 |
| `NotSet` | 0 |

Все исследованные вынесенные свойства используют только оранжевый режим.
