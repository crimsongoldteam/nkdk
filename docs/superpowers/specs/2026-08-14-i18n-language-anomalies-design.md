# Языковые аномалии локализованных XML-строк

## Контекст

Локализованная строка 1С хранится в XML как упорядоченная коллекция
`v8:item`, где каждый элемент содержит пару `v8:lang` и `v8:content`.
Объекты `Language` конфигурации задают зарегистрированные коды и их порядок,
но XML может содержать текст для кода, которому объект `Language` не
соответствует.

Документация 1С подтверждает, что локализованные строки связываются с языком по
коду. После изменения или удаления кода старые пары остаются в строках и
становятся недоступными через обычный редактор, но не перестают быть допустимым
XML: <https://its.1c.ru/db/content/metod8dev/src/developers/platform/metod/devtools/i8102288.htm>.

Проверка 35 конфигураций из `/Users/nikita/git/round-trip-compact/cf`, кроме
`all`, показала:

- 29 конфигураций содержат незарегистрированные языковые коды;
- найдено 4 796 языковых элементов с такими кодами;
- в Storekeeper зарегистрирован только `ru`, но присутствует 61 элемент `en`;
- три локализованные строки в двух конфигурациях нарушают порядок двух
  зарегистрированных языков: две в SMTL и одна в ERP.

Дополнительно обнаружен 121 элемент с кодом `#` и 70 элементов с пустым кодом.
Они не классифицировались как языковые коды в этом исследовании и исключены из
приведённых выше 29 конфигураций и 4 796 элементов.

Импорт не должен удалять или молча нормализовывать эти состояния. YAML должен
сохранять их явно и классифицировать причину.

## Термины

**Зарегистрированный язык** — код `LanguageCode` объекта `Language`,
перечисленного в `Configuration.xml`.

**Канонический порядок** — сначала зарегистрированные языки в порядке
`Language` из `Configuration.xml`, затем незарегистрированные языки в порядке
YAML.

**Сворачиваемое значение** — значение свойства с
`excludeIfEqualNameYAML: true`, текст основного языка которого однозначно
восстанавливается из имени через `canConvertToPascalCase` и
`splitPascalCase`.

## Новые категории `!xml`

Эта спецификация расширяет закрытый словарь из
[`2026-08-14-classified-xml-anomaly-tags-design.md`](2026-08-14-classified-xml-anomaly-tags-design.md).
До реализации `!xml/language` и `!xml/order` остаются неизвестными тегами и
должны отклоняться текущим парсером.

### `!xml/language`

Тег ставится на scalar-значение конкретного языка, если его код отсутствует
среди зарегистрированных языков конфигурации:

```yaml
Заголовок:
  en: !xml/language Buttons
```

Код языка остаётся YAML-ключом, payload тега содержит исходный текст. Тег не
означает ошибку XML: он классифицирует сохранённый текст, недоступный через
текущий набор объектов `Language`.

### `!xml/order`

Тег ставится на mapping всей локализованной строки, если порядок языков в XML
отличается от канонического:

```yaml
Заголовок: !xml/order
  en: Other text
  ru: Другой текст
```

Тег относится к коллекции, а не к отдельному тексту. Порядок ключей mapping
становится исходным порядком `v8:item`. Это первое согласованное применение
классифицированного XML-тега к mapping; scalar-механизм недостаточен, потому
что нарушение относится к взаимному расположению элементов.

`!xml/language` и `!xml/order` независимы и могут применяться одновременно:

```yaml
Заголовок: !xml/order
  en: !xml/language Dont exit
```

Для сворачиваемого основного языка отсутствие его ключа обычно означает, что
значение присутствует в XML и восстанавливается из имени. Под `!xml/order`
такое восстановленное значение размещается после явно перечисленных языков —
это сохраняет исследованный случай `en → ru`, не раскрывая вычисляемый русский
текст в YAML.

## Пустая строка языка

Для сворачиваемого свойства пустая строка основного языка означает, что
соответствующего `v8:item` в XML нет и вычисляемое значение восстанавливать не
нужно:

```yaml
Заголовок:
  en: !xml/language Buttons
  ru: ""
```

Для свойства без сворачивания пустая строка языка также не создаёт `v8:item`,
но не участвует в вычислении значения из имени. Настоящий пустой
`v8:content` в исследованном корпусе, кроме `all`, не найден; отдельное
представление для него в этой спецификации не вводится.

## Архитектурные границы

Проектный реестр языков строится из порядка `Language` в `Configuration.xml` и
`LanguageCode` соответствующих объектов `Language`. Он является concrete-
зависимостью metadata-композиции и передаётся обработчикам `I8nText` и
`FormattedI8nText` через отдельную capability. Общий `ConfigurationContext` и
базовые типы `PropertyRule` не получают новых полей.

Runtime YAML расширяет существующий sidecar классифицированных тегов поддержкой
mapping-тега. Runtime знает только категорию и YAML-узел; он не знает о языках,
`I8nText`, `Language` или каноническом порядке. При копировании mapping sidecar
переносится так же, как уже переносится scalar sidecar.

Классификация языков и порядка остаётся в concrete-модуле локализованных
текстов. Проектная validation получает тот же реестр языков через metadata-
композицию; нейтральные слои validation и projectState не получают частных
условий по `Language` или YAML-ключам языков.

## Матрица представлений

Во всех примерах основной язык — `ru`. Если `en` зарегистрирован, порядок
объектов `Language` равен `ru → en`. Имя сворачиваемого элемента —
`НеВыходить`, вычисляемый русский текст — `Не выходить`.

| Случай | XML | Сворачиваемый | YAML |
|---|---|:---:|---|
| Зарегистрированные `ru → en`, канонический порядок | `<Title><v8:item><v8:lang>ru</v8:lang><v8:content>Не выходить</v8:content></v8:item><v8:item><v8:lang>en</v8:lang><v8:content>Dont exit</v8:content></v8:item></Title>` | Да | <code>Заголовок:<br>&nbsp;&nbsp;en: Dont exit</code> |
| Основной `ru` отсутствует, присутствует зарегистрированный `en` | `<Title><v8:item><v8:lang>en</v8:lang><v8:content>Dont exit</v8:content></v8:item></Title>` | Да | <code>Заголовок:<br>&nbsp;&nbsp;en: Dont exit<br>&nbsp;&nbsp;ru: ""</code> |
| Зарегистрированные `en → ru`, порядок нарушен | `<Title><v8:item><v8:lang>en</v8:lang><v8:content>Dont exit</v8:content></v8:item><v8:item><v8:lang>ru</v8:lang><v8:content>Не выходить</v8:content></v8:item></Title>` | Да | <code>Заголовок: !xml/order<br>&nbsp;&nbsp;en: Dont exit</code> |
| Зарегистрированный `ru`, затем незарегистрированный `en` | `<Title><v8:item><v8:lang>ru</v8:lang><v8:content>Не выходить</v8:content></v8:item><v8:item><v8:lang>en</v8:lang><v8:content>Dont exit</v8:content></v8:item></Title>` | Да | <code>Заголовок:<br>&nbsp;&nbsp;en: !xml/language Dont exit</code> |
| Незарегистрированный `en` перед зарегистрированным `ru` | `<Title><v8:item><v8:lang>en</v8:lang><v8:content>Dont exit</v8:content></v8:item><v8:item><v8:lang>ru</v8:lang><v8:content>Не выходить</v8:content></v8:item></Title>` | Да | <code>Заголовок: !xml/order<br>&nbsp;&nbsp;en: !xml/language Dont exit</code> |
| Только незарегистрированный `en`, основной `ru` отсутствует | `<Title><v8:item><v8:lang>en</v8:lang><v8:content>Buttons</v8:content></v8:item></Title>` | Да | <code>Заголовок:<br>&nbsp;&nbsp;en: !xml/language Buttons<br>&nbsp;&nbsp;ru: ""</code> |
| Зарегистрированные `ru → en`, обычные тексты | `<Title><v8:item><v8:lang>ru</v8:lang><v8:content>Другой текст</v8:content></v8:item><v8:item><v8:lang>en</v8:lang><v8:content>Other text</v8:content></v8:item></Title>` | Нет | <code>Заголовок:<br>&nbsp;&nbsp;ru: Другой текст<br>&nbsp;&nbsp;en: Other text</code> |
| Зарегистрированные `en → ru`, обычные тексты, порядок нарушен | `<Title><v8:item><v8:lang>en</v8:lang><v8:content>Other text</v8:content></v8:item><v8:item><v8:lang>ru</v8:lang><v8:content>Другой текст</v8:content></v8:item></Title>` | Нет | <code>Заголовок: !xml/order<br>&nbsp;&nbsp;en: Other text<br>&nbsp;&nbsp;ru: Другой текст</code> |
| Только незарегистрированный `en`, обычный текст | `<Title><v8:item><v8:lang>en</v8:lang><v8:content>Buttons</v8:content></v8:item></Title>` | Нет | <code>Заголовок:<br>&nbsp;&nbsp;en: !xml/language Buttons</code> |
| Зарегистрированный `ru`, затем незарегистрированный `en`, обычные тексты | `<Title><v8:item><v8:lang>ru</v8:lang><v8:content>Текст</v8:content></v8:item><v8:item><v8:lang>en</v8:lang><v8:content>Text</v8:content></v8:item></Title>` | Нет | <code>Заголовок:<br>&nbsp;&nbsp;ru: Текст<br>&nbsp;&nbsp;en: !xml/language Text</code> |
| Незарегистрированный `en` перед зарегистрированным `ru`, обычные тексты | `<Title><v8:item><v8:lang>en</v8:lang><v8:content>Text</v8:content></v8:item><v8:item><v8:lang>ru</v8:lang><v8:content>Текст</v8:content></v8:item></Title>` | Нет | <code>Заголовок: !xml/order<br>&nbsp;&nbsp;en: !xml/language Text<br>&nbsp;&nbsp;ru: Текст</code> |
| Пустое значение языка | Внутри `Title` нет `v8:item` для этого языка | Нет | <code>Заголовок:<br>&nbsp;&nbsp;ru: ""</code> |

Если XML содержит только один зарегистрированный язык, `!xml/order` не нужен:
взаимного порядка языков нет. Если единственный язык незарегистрирован,
применяется только `!xml/language`.

## Импорт XML → YAML

Импорт получает зарегистрированные коды и их порядок из контекста
конфигурации. Для каждой локализованной строки он:

1. сохраняет порядок `v8:item`;
2. помечает текст каждого незарегистрированного кода тегом
   `!xml/language`;
3. сравнивает последовательность с каноническим порядком и помечает mapping
   тегом `!xml/order` при расхождении;
4. применяет сворачивание основного языка только после классификации языка и
   порядка;
5. записывает основной язык как `""`, если он отсутствовал в XML, но без
   маркера был бы восстановлен из имени.

## Экспорт YAML → XML

Экспорт:

1. принимает `!xml/language` только у кода, отсутствующего среди объектов
   `Language`;
2. принимает `!xml/order` только у mapping, порядок которого отличается от
   канонического;
3. без `!xml/order` создаёт `v8:item` в каноническом порядке;
4. с `!xml/order` сохраняет порядок mapping, а свёрнутый основной язык в
   исследованном варианте добавляет после явно перечисленных языков;
5. не создаёт `v8:item` для пустой строки языка;
6. восстанавливает свёрнутый основной текст из имени только при отсутствии
   запрещающего значения `""`.

## Validation и JSON Schema

- Незарегистрированный код без `!xml/language` — ошибка с путём до значения
  языка.
- `!xml/language` у зарегистрированного кода — избыточный тег и ошибка.
- Неканонический порядок без `!xml/order` — ошибка на mapping локализованной
  строки.
- `!xml/order` при каноническом порядке — избыточный тег и ошибка.
- Оба тега проверяются независимо, поэтому одна строка может получить две
  диагностики.
- `!xml/order` разрешён только на mapping, `!xml/language` — только на scalar
  текста языка.
- JSON Schema описывает допустимые формы scalar- и mapping-тегов, но не
  проверяет порядок ключей объекта: стандартная семантика JSON Schema порядок
  не сохраняет.
- Фактическую регистрацию кода, избыточность тегов и порядок проверяет
  проектный validation-проход по разобранному YAML с sidecar и insertion order.
- Общая schema типа `I8nText` не определяет зарегистрированные коды без
  контекста проекта.

## Границы

- Теги применяются к `I8nText` и текстовой части `FormattedI8nText`.
- XML-фикстуры не изменяются.
- Незарегистрированный язык не удаляется и не преобразуется в
  зарегистрированный.
- Порядок языков не хранится в reference XML или `configuration-index.bin`:
  его полностью выражает YAML mapping и `!xml/order`.
- Служебные значения `v8:lang`, не являющиеся языковыми кодами локализованного
  текста, не классифицируются этим договором.
- Новое представление настоящего пустого `v8:content` не входит в задачу.
- Другие категории `!xml` эта спецификация не меняет.

## Проверки

Нужны точечные тесты:

- XML → YAML и YAML → XML для каждой строки матрицы;
- сочетание `!xml/order` и `!xml/language`;
- разбор, сериализация и копирование mapping-sidecar `!xml/order`;
- сворачиваемый основной язык: присутствует, отсутствует и расположен после
  другого языка;
- зарегистрированный и незарегистрированный код с правильным и избыточным
  тегом;
- канонический и неканонический порядок с правильным и избыточным тегом;
- JSON Schema допустимых форм тегов и проектный validation с реестром языков;
- отсутствие проверки порядка обычным JSON Schema object;
- отсутствие языковых диагностик без проектного контекста;
- round-trip Storekeeper, SMTL и ERP на исследованных примерах.

Перед завершением реализации выполняются `pnpm test`, `pnpm test:e2e`,
архитектурные проверки и `pnpm duplicates -- --base <commit>` согласно
проектным инструкциям.
