# Заготовки фикстур для неподдержанных applied-объектов

## Цель

Подготовить XML-фикстуры и каталог `sync` для applied-объектов из `/Users/nikita/git/roundTripElements`, которых ещё нет в `packages/core/metadata/appliedObjects`.

Работа создаёт только основу для будущих реализаций metadataItem. Она не добавляет `rules.ts`, типы, регистрации, тесты и YAML-контракт.

## Границы

Уже поддержанные каталоги исключаются:

- `Catalogs` -> `metadataCatalog`
- `Documents` -> `metadataDocument`
- `DocumentNumerators` -> `metadataDocumentNumerator`
- `Enums` -> `metadataEnumeration`
- `Sequences` -> `metadataSequence`

Остальные applied-каталоги из выгрузки считаются кандидатами на заготовки, если пользователь явно разметит XML-файлы для переноса.

Служебные и не applied-каталоги не переносятся: `.git`, `ext`, `.DS_Store`, `Configuration.xml`, `ConfigDumpInfo.xml`.

## Разметка фикстур

Автоматическое правило `*ВсеСвойства* -> full.xml`, `*ПоУмолчанию -> minimal.xml` не используется как источник истины: в выгрузке есть типы без файла `ВсеСвойства` и типы с несколькими содержательными вариантами.

Перед созданием файлов пользователь размечает кандидаты в формате:

```text
Константа1 - исключить
КонстантаВсеСвойства - full.xml
КонстантаПоУмолчанию - minimal.xml
```

Допустимые действия:

- `full.xml` — копировать в `__fixtures__/full.xml` и в `__fixtures__/sync/xml/<Имя>.xml`;
- `minimal.xml` — копировать в `__fixtures__/minimal.xml`;
- `исключить` — не переносить этот XML;
- `sync.xml` — использовать только как `__fixtures__/sync/xml/<Имя>.xml`, если full-фикстура должна быть другой.

Если для одного типа выбрано несколько `full.xml`, создаётся несколько каталогов заготовок с суффиксом по имени XML-файла. Если выбран один `full.xml` и один `minimal.xml`, они попадают в один каталог типа.

## Кандидаты для разметки

### AccountingRegisters

- РегистрБухгалтерии1 - указать
- РегистрБухгалтерииВсеСвойстваОбороты - указать
- РегистрБухгалтерииПоУмолчанию - указать

### AccumulationRegisters

- РегистрНакопления1 - указать
- РегистрНакопленияВсеСвойстваОбороты - указать
- РегистрНакопленияПоУмолчанию - указать

### Bots

- БотВсеСвойства - указать
- БотПоУмолчанию - указать

### BusinessProcesses

- БизнесПроцесс1 - указать
- БизнесПроцессВсеСвойства - указать
- БизнесПроцессПоУмолчанию - указать

### CalculationRegisters

- РегистрРасчета1 - указать
- РегистрРасчетаВсеСвойства - указать
- РегистрРасчетаПоУмолчанию - указать

### ChartsOfAccounts

- ПланСчетов1 - указать
- ПланСчетовВсеСвойства - указать
- ПланСчетовПоУмолчанию - указать

### ChartsOfCalculationTypes

- ПланВидовРасчета1 - указать
- ПланВидовРасчетаПоУмолчанию - указать
- ПланРасчетаВсеСвойства - указать

### ChartsOfCharacteristicTypes

- ВидыСубконто - указать
- ПланВидовХарактеристик1 - указать
- ПланВидовХарактеристикВсеСвойства - указать
- ПланВидовХарактеристикПоУмолчанию - указать
- ХарактеристикиОбъектов - указать

### CommandGroups

- ГруппаКомандВсеСвойства - указать
- ГруппаКомандПоУмолчанию - указать

### CommonAttributes

- ОбщийРеквизитВсеСвойства - указать
- ОбщийРеквизитПоУмолчанию - указать

### CommonCommands

- КомандаСДанными - указать

### CommonForms

- ДинамическийСписок - указать
- Команды - указать
- КонстантаВсеСвойства - указать
- ПараметрФормыБезТипа - указать
- РеквизитБезТипа - указать
- СписокЗначений - указать
- УсловноеОформление - указать
- Форма - указать
- ФормаВариантаОтчета - указать
- ФормаВыбораПользователейСистемыВзаимодействия - указать
- ФормаДанныхВерсииИсторииДанных - указать
- ФормаИсторииИзмененийИсторииДанных - указать
- ФормаНастроекДинамическогоСписка - указать
- ФормаНастроекОтчета - указать
- ФормаОтчета - указать
- ФормаПоиска - указать
- ФормаРазличийВерсийИсторииДанных - указать
- Additional - указать
- Button - указать
- CalendarField - указать
- ChartField - указать
- CheckBoxField - указать
- ColumnGroup - указать
- CommandBar - указать
- DendrogramField - указать
- ExtendedTooltip - указать
- FormattedDocumentField - указать
- FormDecoraction - указать
- GanttChartField - указать
- GeographicalSchemaField - указать
- GraphicalSchemaField - указать
- HTMLDocumentField - указать
- InputField - указать
- LabelField - указать
- Pages - указать
- PDFDocumentField - указать
- PeriodField - указать
- PictureField - указать
- PlannerField - указать
- ProgressBarField - указать
- RadioButtonField - указать
- SpreadSheetDocumentField - указать
- Table - указать
- TextDocumentField - указать
- TrackBarField - указать
- TwoTablesAttributes - указать
- UsualGroup - указать

### CommonModules

- ОбщийМодульПодпискаНаСобытие - указать
- ОбщийМодульРегЗадание - указать

### CommonPictures

- ОбщаяКартинкаВсеСвойства - указать
- ОбщаяКартинкаОднаКартинка - указать
- ОбщаяКартинкаПоУмолчанию - указать
- ОбщаяКартинкаРежимКоллекции - указать

### CommonTemplates

- ДополнительныйСловарьПолнотекстовогоПоиска - указать
- ТабличныйДокументВсеСвойства - указать
- ТабличныйДокументПоУмолчанию - указать

### Constants

- Константа1 - указать
- КонстантаБулево - указать
- КонстантаВсеСвойства - указать
- КонстантаПоУмолчанию - указать
- ЭкранированиеВСинониме - указать

### DataProcessors

- Обработка1 - указать
- ОбработкаВсеСвойства - указать
- ОбработкаПоУмолчанию - указать

### DefinedTypes

- ОпределяемыйТипВсеСвойства - указать
- ОпределяемыйТипПоУмолчанию - указать

### DocumentJournals

- ЖурналДокументов1 - указать
- ЖурналДокументовВсеСвойства - указать
- ЖурналДокументовПоУмолчанию - указать

### EventSubscriptions

- ПодпискаНаСобытиеВсеСвойства - указать
- ПодпискаНаСобытиеПоУмолчанию - указать

### ExchangePlans

- ПланОбменаВсеСвойства - указать
- ПланОбменаПоУмолчанию - указать

### ExternalDataSources

- ВнешнийИсточникДанныхВсеСвойства - указать
- ВнешнийИсточникДанныхПоУмолчанию - указать

### FilterCriteria

- КритерийОтбораВсеСвойства - указать
- КритерийОтбораПоУмолчанию - указать

### FunctionalOptions

- ФункциональнаяОпцияБулево - указать
- ФункциональнаяОпцияВсеСвойства - указать
- ФункциональнаяОпцияПоУмолчанию - указать
- ФункциональныеОпцииПолный - указать

### FunctionalOptionsParameters

- ПараметрФункциональныхОпцийВсеСвойства - указать
- ПараметрФункциональныхОпцийПоУмолчанию - указать

### HTTPServices

- HTTPСервисВсеСвойства - указать
- HTTPСервисПоУмолчанию - указать

### InformationRegisters

- ЗначенияХарактеристикОбъектов - указать
- РегистрСведений1 - указать
- РегистрСведенийАдресация - указать
- РегистрСведенийВсеСвойстваНезависимый - указать
- РегистрСведенийГрафик - указать
- РегистрСведенийКомандныйИнтерфейс - указать
- РегистрСведенийКомандныйИнтерфейс1 - указать
- РегистрСведенийПодчиненРегистратору - указать
- РегистрСведенийПоУмолчанию - указать

### IntegrationServices

- СервисИнтеграцииВсеСвойства - указать

### Languages

- Английский - указать
- Русский - указать

### Reports

- ОтчетСКД - указать

### Roles

- Администратор - указать
- РольВсеСвойства - указать
- РольПоУмолчанию - указать

### ScheduledJobs

- РегламентноеЗаданиеВсеСвойства - указать
- РегламентноеЗаданиеПоУмолчанию - указать

### SessionParameters

- ПараметрСеансаВсеСвойства - указать
- ПараметрСеансаПоУмолчанию - указать
- ПараметрСеансаТекущийИсполнитель - указать

### SettingsStorages

- ХранилищеНастроекВсеСвойства - указать
- ХранилищеНастроекПоУмолчанию - указать

### StyleItems

- ЭлементСтиляРамка - указать
- ЭлементСтиляЦвет - указать
- ЭлементСтиляШрифтВсеСвойства - указать

### Styles

- СтильВсеСвойства - указать
- СтильПоУмолчанию - указать

### Subsystems

- ПодсистемаВсеСвойства - указать
- ПодсистемаИспользуетсяВПримерах - указать
- ПодсистемаПоУмолчанию - указать

### Tasks

- Задача1 - указать
- ЗадачаВсеСвойства - указать
- ЗадачаПоУмолчанию - указать

### WSReferences

- WSСсылкаВсеСвойства - указать
- WSСсылкаПоУмолчанию - указать

### WebServices

- WebСервисВсеСвойства - указать
- WebСервисПоУмолчанию - указать

### XDTOPackages

- ПакетXDTO1 - указать

## Структура результата

Для каждого неподдержанного объекта создаётся каталог:

```text
packages/core/metadata/appliedObjects/<metadataName>/
  __fixtures__/
    full.xml
    minimal.xml
    sync/
      data.ts
      xml/<ИмяВсеСвойства>.xml
      xml/<ИмяВсеСвойства>/...
      nkdk/<ИмяВсеСвойства>/Свойства.yaml
```

`full.xml`, `minimal.xml` и `sync/xml` берутся только из явной разметки. Если минимальной фикстуры нет, файл не создаётся, а объект попадает в итоговый отчёт.

`sync/xml/<ИмяВсеСвойства>.xml` получает тот же XML, что и `full.xml`. Если рядом с sync-объектом есть связанные XML-файлы и внешние файлы в одноимённой папке (`Ext`, `Forms`, `Templates`, `Commands`, `Help`), они копируются в `sync/xml/<ИмяВсеСвойства>/...` с сохранением структуры.

`sync/nkdk/<ИмяВсеСвойства>/Свойства.yaml` остаётся пустым. `sync/data.ts` экспортирует пустую строку, потому что YAML-структура появится только после реализации правил объекта и прохождения XML-цикла.

## Имена каталогов

Каталоги в `appliedObjects` именуются в стиле существующего кода: `metadata` + единственное английское имя объекта, например `metadataDocumentJournal` для `DocumentJournals`.

Если однозначное единственное имя нельзя вывести из существующей схемы, имя фиксируется консервативно по названию XML-каталога и отмечается в отчёте для проверки перед реализацией объекта.

## Проверка

После создания заготовок нужно проверить:

- `git status --short` показывает только новые каталоги фикстур и сгенерированные ранее Langium-файлы, если они изменились при базовой подготовке;
- в заготовках нет `.DS_Store`;
- для каждого объекта есть `sync/data.ts` и пустой `Свойства.yaml`;
- XML-файлы из выгрузки скопированы без изменения содержимого.

Полный `pnpm test` для самих заготовок не обязателен, потому что тесты и регистрации не добавляются. Базовый прогон в рабочем дереве уже выполнен до изменений.
