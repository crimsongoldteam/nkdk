# Заготовки фикстур для неподдержанных applied-объектов

## Цель

Подготовить XML-фикстуры и каталог `sync` для applied-объектов из `/Users/nikita/git/roundTripElements`, которых ещё нет в `packages/core/metadata/appliedObjects`.

Работа создаёт только основу для будущих реализаций metadataItem. Она не добавляет `rules.ts`, типы, регистрации, тесты и YAML-контракт.

## Границы

Уже поддержанные каталоги исключаются:

- `Catalogs` -> `metadataCatalog`
- `Documents` -> `metadataDocument`
- `DocumentNumerators` -> `metadataDocumentNumerator`
- `Enums` -> `metadataEnumeration`
- `Sequences` -> `metadataSequence`

Остальные applied-каталоги из выгрузки считаются кандидатами на заготовки, если пользователь явно разметит XML-файлы для переноса.

Служебные и не applied-каталоги не переносятся: `.git`, `ext`, `.DS_Store`, `Configuration.xml`, `ConfigDumpInfo.xml`.

## Разметка фикстур

Автоматическое правило `*ВсеСвойства* -> full.xml`, `*ПоУмолчанию -> minimal.xml` не используется как источник истины: в выгрузке есть типы без файла `ВсеСвойства` и типы с несколькими содержательными вариантами.

Перед созданием файлов пользователь размечает кандидаты в формате:

```text
Константа1 - исключить
КонстантаВсеСвойства - full.xml
КонстантаПоУмолчанию - minimal.xml
```

Допустимые действия:

- любое имя вида `<name>.xml` — копировать исходный XML в `__fixtures__/<name>.xml`;
- `full.xml` — обычное имя для полной XML-фикстуры объекта;
- `minimal.xml` — копировать в `__fixtures__/minimal.xml`;
- `исключить` — не переносить этот XML.

Все строки, где действие не равно `исключить`, становятся XML-фикстурами в каталоге объекта. Для одного типа можно выбрать несколько XML-файлов; они попадают в один каталог `__fixtures__` под именами из правой части разметки.

Для `sync/xml` среди включённых файлов выбирается источник, в имени которого есть `ВсеСвойства`: это самый насыщенный вариант объекта. Если таких файлов несколько, выбор фиксируется в итоговом отчёте. Если включённых файлов с `ВсеСвойства` нет, `sync` для этого типа не создаётся до ручного выбора источника.

## Кандидаты для разметки

Ниже указана предварительная разметка по имени. Пользователь проверяет её перед созданием файлов и может заменить любое действие на имя XML-файла для `__fixtures__` или на `исключить`.

### AccountingRegisters

- РегистрБухгалтерии1 - исключить
- РегистрБухгалтерииВсеСвойстваОбороты - full.xml
- РегистрБухгалтерииПоУмолчанию - minimal.xml

### AccumulationRegisters

- РегистрНакопления1 - исключить
- РегистрНакопленияВсеСвойстваОбороты - full.xml
- РегистрНакопленияПоУмолчанию - minimal.xml

### Bots

- БотВсеСвойства - full.xml
- БотПоУмолчанию - minimal.xml

### BusinessProcesses

- БизнесПроцесс1 - исключить
- БизнесПроцессВсеСвойства - full.xml
- БизнесПроцессПоУмолчанию - minimal.xml

### CalculationRegisters

- РегистрРасчета1 - исключить
- РегистрРасчетаВсеСвойства - full.xml
- РегистрРасчетаПоУмолчанию - minimal.xml

### ChartsOfAccounts

- ПланСчетов1 - исключить
- ПланСчетовВсеСвойства - full.xml
- ПланСчетовПоУмолчанию - minimal.xml

### ChartsOfCalculationTypes

- ПланВидовРасчета1 - исключить
- ПланВидовРасчетаПоУмолчанию - minimal.xml
- ПланРасчетаВсеСвойства - full.xml

### ChartsOfCharacteristicTypes

- ВидыСубконто - исключить
- ПланВидовХарактеристик1 - исключить
- ПланВидовХарактеристикВсеСвойства - full.xml
- ПланВидовХарактеристикПоУмолчанию - minimal.xml
- ХарактеристикиОбъектов - исключить

### CommandGroups

- ГруппаКомандВсеСвойства - full.xml
- ГруппаКомандПоУмолчанию - minimal.xml

### CommonAttributes

- ОбщийРеквизитВсеСвойства - full.xml
- ОбщийРеквизитПоУмолчанию - minimal.xml

### CommonCommands

- КомандаСДанными - исключить

### CommonForms

- ДинамическийСписок - исключить
- Команды - исключить
- КонстантаВсеСвойства - const.xml
- ПараметрФормыБезТипа - исключить
- РеквизитБезТипа - исключить
- СписокЗначений - исключить
- УсловноеОформление - исключить
- Форма - исключить
- ФормаВариантаОтчета - reportOption.xml
- ФормаВыбораПользователейСистемыВзаимодействия - dialog.xml
- ФормаДанныхВерсииИсторииДанных - versioning.xml
- ФормаИсторииИзмененийИсторииДанных - story.xml
- ФормаНастроекДинамическогоСписка - dynamicList.xml
- ФормаНастроекОтчета - reportSettings.xml
- ФормаОтчета - report.xml
- ФормаПоиска - search.xml
- ФормаРазличийВерсийИсторииДанных - changesStory.xml
- Additional - исключить
- Button - исключить
- CalendarField - исключить
- ChartField - исключить
- CheckBoxField - исключить
- ColumnGroup - исключить
- CommandBar - исключить
- DendrogramField - исключить
- ExtendedTooltip - исключить
- FormattedDocumentField - исключить
- FormDecoraction - исключить
- GanttChartField - исключить
- GeographicalSchemaField - исключить
- GraphicalSchemaField - исключить
- HTMLDocumentField - исключить
- InputField - исключить
- LabelField - исключить
- Pages - исключить
- PDFDocumentField - исключить
- PeriodField - исключить
- PictureField - исключить
- PlannerField - исключить
- ProgressBarField - исключить
- RadioButtonField - исключить
- SpreadSheetDocumentField - исключить
- Table - исключить
- TextDocumentField - исключить
- TrackBarField - исключить
- TwoTablesAttributes - исключить
- UsualGroup - исключить

### CommonModules

- ОбщийМодульПодпискаНаСобытие - исключить
- ОбщийМодульРегЗадание - исключить

### CommonPictures

- ОбщаяКартинкаВсеСвойства - full.xml
- ОбщаяКартинкаОднаКартинка - single.xml
- ОбщаяКартинкаПоУмолчанию - minimal.xml
- ОбщаяКартинкаРежимКоллекции - collection.xml

### CommonTemplates

- ДополнительныйСловарьПолнотекстовогоПоиска - исключить
- ТабличныйДокументВсеСвойства - full.xml
- ТабличныйДокументПоУмолчанию - minimal.xml

### Constants

- Константа1 - исключить
- КонстантаБулево - исключить
- КонстантаВсеСвойства - full.xml
- КонстантаПоУмолчанию - minimal.xml
- ЭкранированиеВСинониме - исключить

### DataProcessors

- Обработка1 - исключить
- ОбработкаВсеСвойства - full.xml
- ОбработкаПоУмолчанию - minimal.xml

### DefinedTypes

- ОпределяемыйТипВсеСвойства - full.xml
- ОпределяемыйТипПоУмолчанию - minimal.xml

### DocumentJournals

- ЖурналДокументов1 - исключить
- ЖурналДокументовВсеСвойства - full.xml
- ЖурналДокументовПоУмолчанию - minimal.xml

### EventSubscriptions

- ПодпискаНаСобытиеВсеСвойства - full.xml
- ПодпискаНаСобытиеПоУмолчанию - minimal.xml

### ExchangePlans

- ПланОбменаВсеСвойства - full.xml
- ПланОбменаПоУмолчанию - minimal.xml

### ExternalDataSources

- ВнешнийИсточникДанныхВсеСвойства - full.xml
- ВнешнийИсточникДанныхПоУмолчанию - minimal.xml

### FilterCriteria

- КритерийОтбораВсеСвойства - full.xml
- КритерийОтбораПоУмолчанию - minimal.xml

### FunctionalOptions

- ФункциональнаяОпцияБулево - исключить
- ФункциональнаяОпцияВсеСвойства - full.xml
- ФункциональнаяОпцияПоУмолчанию - minimal.xml
- ФункциональныеОпцииПолный - исключить

### FunctionalOptionsParameters

- ПараметрФункциональныхОпцийВсеСвойства - full.xml
- ПараметрФункциональныхОпцийПоУмолчанию - minimal.xml

### HTTPServices

- HTTPСервисВсеСвойства - full.xml
- HTTPСервисПоУмолчанию - minimal.xml

### InformationRegisters

- ЗначенияХарактеристикОбъектов - исключить
- РегистрСведений1 - исключить
- РегистрСведенийАдресация - исключить
- РегистрСведенийВсеСвойстваНезависимый - full.xml
- РегистрСведенийГрафик - исключить
- РегистрСведенийКомандныйИнтерфейс - исключить
- РегистрСведенийКомандныйИнтерфейс1 - исключить
- РегистрСведенийПодчиненРегистратору - reg.xml
- РегистрСведенийПоУмолчанию - minimal.xml

### IntegrationServices

- СервисИнтеграцииВсеСвойства - full.xml

### Languages

- Английский - en.xml
- Русский - ru.xml

### Reports

- ОтчетСКД - исключить

### Roles

- Администратор - исключить
- РольВсеСвойства - full.xml
- РольПоУмолчанию - minimal.xml

### ScheduledJobs

- РегламентноеЗаданиеВсеСвойства - full.xml
- РегламентноеЗаданиеПоУмолчанию - minimal.xml

### SessionParameters

- ПараметрСеансаВсеСвойства - full.xml
- ПараметрСеансаПоУмолчанию - minimal.xml
- ПараметрСеансаТекущийИсполнитель - исключить

### SettingsStorages

- ХранилищеНастроекВсеСвойства - full.xml
- ХранилищеНастроекПоУмолчанию - minimal.xml

### StyleItems

- ЭлементСтиляРамка - border.xml
- ЭлементСтиляЦвет - color.xml
- ЭлементСтиляШрифтВсеСвойства - font.xml

### Styles

- СтильВсеСвойства - full.xml
- СтильПоУмолчанию - minimal.xml

### Subsystems

- ПодсистемаВсеСвойства - full.xml
- ПодсистемаИспользуетсяВПримерах - исключить
- ПодсистемаПоУмолчанию - minimal.xml

### Tasks

- Задача1 - исключить
- ЗадачаВсеСвойства - full.xml
- ЗадачаПоУмолчанию - minimal.xml

### WSReferences

- WSСсылкаВсеСвойства - full.xml
- WSСсылкаПоУмолчанию - minimal.xml

### WebServices

- WebСервисВсеСвойства - full.xml
- WebСервисПоУмолчанию - minimal.xml

### XDTOPackages

- ПакетXDTO1 - исключить

## Структура результата

Для каждого неподдержанного объекта создаётся каталог:

```text
packages/core/metadata/appliedObjects/<metadataName>/
  __fixtures__/
    full.xml
    minimal.xml
    <другие-явно-размеченные>.xml
    sync/
      data.ts
      xml/<ИмяВсеСвойства>.xml
      xml/<ИмяВсеСвойства>/...
      nkdk/<ИмяВсеСвойства>/Свойства.yaml
```

Файлы в `__fixtures__` берутся только из явной разметки. Если минимальной фикстуры нет, файл не создаётся, а объект попадает в итоговый отчёт.

`sync/xml/<ИмяВсеСвойства>.xml` получает XML включённого файла, в исходном имени которого есть `ВсеСвойства`. Если рядом с sync-объектом есть связанные XML-файлы и внешние файлы в одноимённой папке (`Ext`, `Forms`, `Templates`, `Commands`, `Help`), они копируются в `sync/xml/<ИмяВсеСвойства>/...` с сохранением структуры.

`sync/nkdk/<ИмяВсеСвойства>/Свойства.yaml` остаётся пустым. `sync/data.ts` экспортирует пустую строку, потому что YAML-структура появится только после реализации правил объекта и прохождения XML-цикла.

## Имена каталогов

Каталоги в `appliedObjects` именуются в стиле существующего кода: `metadata` + единственное английское имя объекта, например `metadataDocumentJournal` для `DocumentJournals`.

Если однозначное единственное имя нельзя вывести из существующей схемы, имя фиксируется консервативно по названию XML-каталога и отмечается в отчёте для проверки перед реализацией объекта.

## Проверка

После создания заготовок нужно проверить:

- `git status --short` показывает только новые каталоги фикстур и сгенерированные ранее Langium-файлы, если они изменились при базовой подготовке;
- в заготовках нет `.DS_Store`;
- для каждого объекта есть `sync/data.ts` и пустой `Свойства.yaml`;
- XML-файлы из выгрузки скопированы без изменения содержимого.

Полный `pnpm test` для самих заготовок не обязателен, потому что тесты и регистрации не добавляются. Базовый прогон в рабочем дереве уже выполнен до изменений.
