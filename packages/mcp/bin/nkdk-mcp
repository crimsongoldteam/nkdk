#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT="$(cd "${SCRIPT_DIR}/../../.." && pwd)"

exec pnpm --dir "$ROOT" --filter @nkdk/mcp exec tsx --tsconfig tsconfig.json src/server.ts "$@"
