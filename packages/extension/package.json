{
    "name": "vscode-nkdk-test",
    "description": "The extension specific package",
    "version": "0.0.1",
    "displayName": "nkdk-test",
    "type": "module",
    "engines": {
        "vscode": "^1.67.0"
    },
    "categories": [
        "Programming Languages"
    ],
    "contributes": {
        "languages": [{
            "id": "nkdk",
            "aliases": ["nkdk", "nkdk"],
            "extensions": [".nkdk"],
            "configuration": "./language-configuration.json"
        }],
        "grammars": [{
            "language": "nkdk",
            "scopeName": "source.nkdk",
            "path": "syntaxes/nkdk.tmLanguage.json"
        }]
    },
    "activationEvents": [
        "onLanguage:nkdk"
    ],
    "main": "./out/extension/main.cjs",
    "scripts": {
        "clean": "shx rm -fr *.tsbuildinfo out syntaxes",
        "vscode:prepublish": "npm run build",
        "build:prepare": "shx mkdir -p ./syntaxes/ && shx cp -f ../language/syntaxes/nkdk.tmLanguage.json ./syntaxes/nkdk.tmLanguage.json",
        "build": "npm run build:prepare && tsc -b tsconfig.json && node esbuild.mjs",
        "build:clean": "npm run clean && npm run build",
        "watch": "npm run build:prepare && concurrently -n tsc,esbuild -c blue,yellow \"tsc -b tsconfig.json --watch\" \"node esbuild.mjs --watch\""
    },
    "dependencies": {
        "langium": "~4.2.0",
        "nkdk-language": "workspace:*",
        "vscode-languageclient": "~9.0.1",
        "vscode-languageserver": "~9.0.1"
    },
    "devDependencies": {
        "@types/vscode": "~1.67.0",
        "concurrently": "~8.2.1",
        "esbuild": "~0.25.5",
        "shx": "~0.3.4"
    },
    "vsce": {
      "dependencies": false
    }
}
