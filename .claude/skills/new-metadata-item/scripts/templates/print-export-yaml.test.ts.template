/**
 * Временный тест: скопировать в каталог объекта (например рядом с `__fixtures__/data.ts`),
 * переименовать в `.test.ts`, подставить импорты и `rule`, запустить из `packages/core`:
 *   pnpm vitest run metadata/.../print-export-yaml-once.test.ts --run
 * Затем удалить файл.
 */
import { describe, it } from "vitest"
import { PropertyRule } from "~/metadata/orchestration"
import { testExportPropertyToYAML } from "~/tests/property/exportPropertyToYAML"
// TODO: импорт side-effect регистрации типов, например:
// import "../types"
// TODO: импорт внутренней константы из data.ts:
// import { fullMyObject } from "./data"

const rule: PropertyRule = {
  // TODO: как в fromYAML.test.ts / toYAML.test.ts объекта
  type: "MyObject",
  yaml: "КлючВКорневомYAML",
}

describe("print export YAML (temporary)", () => {
  it("prints", () => {
    // TODO: подставьте экспорт из data.ts
    const value = undefined as never
    const result = testExportPropertyToYAML({ rule, value })
    // eslint-disable-next-line no-console
    console.log(JSON.stringify(result, null, 2))
  })
})
