# Итоговые исправления по review

Статус: **DONE, включая авторизованную дополнительную волну**

## Что исправлено

1. Полная синхронизация расширения в XML запрещена на обеих публичных
   границах: core и MCP возвращают
   `full_xml_sync_component_not_supported` до чтения Проекта, создания
   XML-каталога и изменения снимка.
2. MCP-подтверждение импорта больше не показывает `cf`, если компонент
   определяет `auto`.
3. Холодный снимок базовой `cf` собирается fact-only командами в уже
   подготовленных физических YAML-worker. Worker сам читает и разбирает
   файл, извлекает индексные факты без схем, валидаторов и второго прохода;
   ошибки чтения, синтаксиса и извлечения прерывают импорт.
4. Индекс конфигурации переведён на формат 2.0. В нём обязательно хранятся
   локальные reference/owner-индексы и локальные зависимости компонента.
   Фрагментный codec переносит этот вклад через worker, детерминированно
   объединяя и сортируя зависимости.
5. XML-import собирает локальный validation-вклад во время первого
   XML → YAML обхода, без повторного чтения созданного YAML. Для `Picture`
   сохраняются только явно квалифицированные ссылки
   `ОбщаяКартинка.*`; стандартные картинки не становятся зависимостями.
6. При импорте `cfe` второй проход использует слоёный снимок
   `local + base`, но на диск записываются только локальные данные
   расширения. Базовые владельцы и индексы в снимок `cfe` не протекают.
7. Полная XML-синхронизация `cf` сохраняет существующий локальный вклад
   индекса 2.0 без изменений.
8. Fact-only режим продолжает собирать валидные ссылки при отключённых
   диагностиках. Договор `metadata/project` при этом не импортирует
   конкретные реализации metadata: типы выводятся из нейтрального
   `LocalYamlFact`.

## RED → GREEN

Исходные дефекты были зафиксированы тестами:

- core-интеграция разрешяла полную XML-синхронизацию `cfe`, а MCP доходил до
  ошибки поиска Проекта вместо раннего доменного отказа;
- подтверждение MCP подставляло `cf` для компонента `auto`;
- четыре проверки prepared-worker показали, что fact-only обход выполнялся
  координатором, а не физическими worker;
- тесты persistence отсутствовали до добавления codec локального shared
  snapshot; сквозной импорт получал `localIndexes: undefined`;
- регрессия full sync обнаружила потерю сохранённых локальных индексов;
- первый полный `pnpm test` выявил устаревшее ожидание пустых ссылочных фактов
  при `validationDiagnostics: false` и запрещённый импорт
  `metadata/project -> commonObjects`.

После исправлений зелёные проверки включают:

- configuration index: 12 файлов / 114 тестов;
- XML-import contribution, worker, pool и snapshot: 5 файлов / 51 тест;
- prepared worker, component reference, full sync и persisted snapshot:
  6 файлов / 37 тестов;
- сквозной импорт реального `cfe`: 1 файл / 1 тест;
- MCP sync/import: 2 файла / 10 тестов;
- финальные регрессии fact-only и архитектурных границ:
  4 файла / 46 тестов.

## Итоговая проверка

- `pnpm --filter @nkdk/core type-check` — успешно.
- `pnpm --filter @nkdk/mcp type-check` — успешно.
- `pnpm test` — успешно:
  - core: 633 файла / 4700 тестов;
  - MCP: 14 файлов / 55 тестов.
- `git diff --check` — замечаний нет.
- В `componentReferenceIndex.ts` отсутствуют чтение YAML, parser,
  extractor и `p-limit`: координатор занимается только обнаружением,
  распределением, объединением и закрытием worker.

## Коммиты

- `ef108ae16` — запрет XML-синхронизации расширений;
- `7c1a87ae9` — корректное подтверждение компонента `auto`;
- `2048f5c3d` — fact-only проход в prepared-worker;
- `3f6bad9f9` — persisted shared snapshot;
- `56b6a7a39` — локальные индексы в формате 2.0;
- `b65fb866c` — локальный validation-вклад XML-import;
- `7944ce8fd` — запись только локального снимка расширения;
- `863e470ff` — сохранение `localIndexes` при XML-синхронизации;
- `6371ffdbb` — ссылочные факты без нарушения архитектурных границ.

Незакрытых замечаний и известных блокирующих рисков нет.

## Авторизованная дополнительная волна

### Что исправлено

1. Идентичность компонента стала обязательной на обеих публичных core-границах
   полной XML-синхронизации: `syncConfigurationToXML` и `planSyncToXml`.
   Обе операции принимают только `componentPath: "cf"` и отклоняют отсутствие
   идентичности либо любой другой компонент до preflight, чтения снимка,
   создания XML-каталога и записи файлов. MCP передаёт разрешённый
   `componentPath` как в apply, так и в plan.
2. `metadata/project/localIndexes.ts` больше не импортирует
   `PictureLibFromYAML` и не содержит знаний о `Picture` либо
   `ОбщаяКартинка.*`. Конкретный отбор ссылок Picture перенесён рядом с
   реализацией Picture в существующую регистрацию
   `collectLocalFactsFromYAML`; общий слой получает результат через
   нейтральный `LocalMetadataFactsWriter`.
3. Существующие обработчики `collectMetadataTargetReferences` не
   переиспользованы: они работают с модельным Picture `{ type, ref }` и
   требуют `ParsedYaml` для диагностик, тогда как локальный XML-import
   передаёт сырой YAML Picture строкой либо объектом `{ Ссылка }`.
   Отдельный registry не создавался.

### RED → GREEN

- Apply без `componentPath` и с `erf/*` доходил до preflight вместо раннего
  отказа; plan без `componentPath`, с `cfe/*` и `erf/*` также доходил до
  preflight. RED-набор дал 5 ожидаемых падений. После исправления все
  12 тестов координатора прошли.
- Архитектурная проверка обнаружила запрещённый импорт
  `metadata/project/localIndexes.ts -> ../systemEnumerations/`. После
  переноса Picture-логики проверка границ прошла; поведение подтверждено
  тестами строкового Picture, `{ Ссылка }` и стандартной картинки.

### Проверка дополнительной волны

- `pnpm --filter @nkdk/core type-check` — успешно.
- `pnpm --filter @nkdk/mcp type-check` — успешно.
- Focused core-набор full sync, local indexes, Picture, XML-import и
  архитектурных границ — 10 файлов / 93 теста.
- Focused MCP sync — 1 файл / 4 теста.
- `pnpm test` — успешно:
  - core: 633 файла / 4707 тестов;
  - MCP: 14 файлов / 55 тестов.
- `git diff --check` — замечаний нет.

### Коммиты дополнительной волны

- `617267fc5` — обязательная идентичность компонента на apply/plan;
- `b12a731e5` — Picture-факты через нейтральную регистрацию типа.

Незакрытых замечаний, блокеров и известных блокирующих рисков после
авторизованной дополнительной волны нет.
