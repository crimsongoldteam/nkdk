# Task 5: общий снимок метаданных и второй проход YAML

## Результат

- Добавлен import-адаптер `createImportSharedMetadata`, который проверяет уникальность логических владельцев и переиспользует двоичный `SharedValidationSnapshot` валидации.
- `FormExportToYAMLContext` принимает готовый `OwnerMetadataCache`; новый путь импорта не передаёт `projectDir` и не читает YAML других объектов.
- Ошибка разрешения `ПутьКДанным` сохраняет исходное значение и формирует неблокирующее предупреждение `unresolved_data_path`.
- Второй проход worker записывает YAML и создаваемые текстовые файлы во временный каталог, возвращает внешние XML-файлы и продолжает работу после ошибки отдельного задания.
- Каждая сохранённая metadata-модель удаляется из состояния worker в `finally`.

## Проверка

- TDD RED: `metadataSnapshot.test.ts` сначала завершился ошибкой из-за отсутствующего `metadataSnapshot.ts`.
- TDD RED: тест предупреждения DataPath сначала получил пустой список предупреждений.
- TDD RED: тест внедрённого owner cache сначала сохранил `Объект.Code` без преобразования.
- TDD RED: тесты второго прохода сначала получили заглушку `Второй проход XML-import будет реализован в Task 5`.
- Целевой прогон: 4 файла, 28 тестов — успешно.
- Полный `pnpm test`: core — 788 файлов / 5328 тестов успешно; MCP — 13 файлов / 38 тестов успешно; CLI — 6 файлов / 58 тестов успешно, один несвязанный тест `src/commands/validate.test.ts:140` завершился по таймауту 5000 мс.
- `pnpm --filter @nkdk/core type-check`: в изменённых файлах ошибок нет; команда остаётся неуспешной из-за существующих ошибок в `rootIO.ts`, `syncToXML.ts`, `projectFiles.ts`, `projectValidationPasses.test.ts` и `yamlFactExtractor.ts`.
- `git diff --check` — успешно.

## Границы

- XML-фикстуры и правила преобразования fromXML/toXML/fromYAML/toYAML не изменялись.
- Несвязанные ошибки проверки типов и таймаут CLI не исправлялись.
